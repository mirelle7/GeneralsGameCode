# Splitscreen — Progress Tracker & Decision Log

Living document. The implementing agent updates this file **every session**:
tick checkboxes, append decisions, record human-checkpoint results. Read this
file at the start of every session to recover state. Docs:
`splitscreen-plan.md` (design) → `splitscreen-plan2.md` (work packages) →
`splitscreen-conventions.md` (idioms, build, testing) → this file (state).
Current baton for the bug sweep: `splitscreen-bugfix-handoff3.md` (what is open, how to test).
`handoff2.md`'s items (5.1-5.5) are still open and untouched by round 4 below; handoff3 is a
separate, newer batch of 13 findings from 11 fresh user reports.

## 1. Environment (fill once, first session)

- [x] Build preset confirmed with user: **`build/win32`** (CMake generator
  `Ninja Multi-Config`, `RTS_BUILD_OPTION_SDL3:BOOL=ON`). Confirmed 2026-07-22;
  `win32-vcpkg-debug` is not used and does not exist. Stop asking.
- [x] Build target name for Zero Hour exe: **`z_generals`**, which produces
  `build/win32/GeneralsMD/Debug/generalszh.exe` (~18 MB, path relative to the
  repo root). Lib targets: `z_gameengine`, `z_gameenginedevice`.
- [ ] Game install / run directory: `____________` — **still unfilled. ASK the
  user for it; do NOT go looking.** Probing the filesystem or the registry for it
  trips a permission prompt, and the user declined that search on 2026-07-30.
- [~] Launch flags for windowed dev testing: `-splitscreendev [n] -win`.
  `-splitscreendev` with NO count puts a real pad on seat 1, which is what the
  input work needs; with a count it creates that many FAKE seats and a real pad
  joining then lands on seat 7 instead (see the 2026-07-27 round-4 note (D)).
  Other useful flags unconfirmed.
- [ ] How INI/data changes reach the game dir: `____________`
- [ ] Local replay-check command (from `check-replays.yml`): `____________`

Build recipe (re-verified 2026-07-30). The `-arch=x86` matters: an x64 dev shell
breaks the link. The Bash shell lacks `midl.exe`/MSVC on PATH, so builds must run
from a VS dev environment:

```powershell
# $vs = the VS BuildTools install root, e.g. from `vswhere -latest -property installationPath`
Import-Module "$vs\Common7\Tools\Microsoft.VisualStudio.DevShell.dll"
Enter-VsDevShell -VsInstallPath $vs -DevCmdArguments "-arch=x86 -host_arch=x86" -SkipAutomaticLocation
Set-Location <repo>\build\win32
& "$vs\Common7\IDE\CommonExtensions\Microsoft\CMake\Ninja\ninja.exe" -f build-Debug.ninja z_generals
```

**Build RELEASE ONLY** (user, 2026-07-31: "stop building debug and just focus on release"). Swap
`build-Debug.ninja` for `build-Release.ninja` in the command above; the exe lands in
`build/win32/GeneralsMD/Release/generalszh.exe` (~9 MB). Debug is roughly twice the build time and
the user does not run it. Note the consequence: `DEBUG_CRASH`/`DEBUG_ASSERTCRASH` are compiled out,
so an assert cannot be the thing that tells you a path failed — that is what the seat overlay
probes are for.

Trap, cost one wasted build (2026-07-31): do NOT pipe ninja through `Select-String ... |
Select-Object -First N`. PowerShell tears the upstream command down the moment the count is
satisfied, so the build is KILLED mid-compile and the next run fails with `fatal error C1083:
Cannot open compiler generated file ... Permission denied` from the orphaned compiler's lock. It
reads exactly like a real build break. Redirect to a file with `*> log.txt` and grep the file, or
let ninja print in full.

## 2. Pre-flight verification — ALL ANSWERED 2026-07-02 (by code reading)

| # | Question | Needed by | Answer |
|---|---|---|---|
| V1 | 2D draw color multiply? | WP3 | **Yes.** `Render2DClass::Add_Quad(..., unsigned long color = 0xFFFFFFFF)` modulates texture by per-quad vertex color (`GeneralsMD/Code/Libraries/Source/WWVegas/WW3D2/render2d.h:118-127`). **No shader needed** — pass the seat color as the quad color. |
| V2 | `winGetWindowFromId(parent, id)` scoping? | WP8 | **Subtree + trailing siblings** (`GameWindowManager.cpp:654-678`): starts at the given window, recurses children, **then walks `m_next` siblings** — a missing child would silently match a later bar instance. → A strict `findChildById(root, id)` helper that recurses only into `root->winGetChild()` is **mandatory** for WP8. Scoped-lookup precedent: `IMEManager.cpp:575-582`. |
| V3 | Slot index → `playerIndex` mapping? | WP5, WP9 | `GameLogic::startNewGame` (`GeneralsMD/.../GameLogic.cpp:1384-1412`): slot *i* becomes side dict with `playerName = "player%d" % slotIdx`; `ThePlayerList->newGame()` builds Players from `TheSidesList`. Recipe: `ThePlayerList->findPlayerWithNameKey(NAMEKEY(fmt("player%d", slotIdx)))->getPlayerIndex()` — same as `NetCommandMsg.cpp:172`. |
| V4 | Is adding `m_seatIndex` serialization-safe? | WP2 | **Yes.** Recorder writes exactly type + playerIndex + args (`Recorder.cpp:711-712` write, `:1338-1340` read); NetCommandMsg likewise reconstructs from type/args/sender. `m_seatIndex` stays client-side; replay/net-received messages default to seat 0, which is correct (they never re-enter client translators). |
| V5 | Mid-game `setPlayerType(PLAYER_COMPUTER, TRUE)`? | WP9 | **Mechanism exists, one known hazard.** `Player.cpp:738-755`: deletes `m_ai`, creates `AISkirmishPlayer(this)`. BUT skirmish AI scripts are duplicated/qualified only at map load in `initFromDict` for slots that are AI *at that time* (`Player.cpp:863-886`); human slots get civilian scripts (`:829-858`). Mid-game human→AI takeover ⇒ AI likely scriptless/passive. Probable fix: when splitscreen enabled, also duplicate+stash skirmish scripts for human slots at load (or run the qualify step at takeover). **Spike B verifies runtime behavior of both flip directions.** |
| V6 | Letterbox state location? | WP6 | **Display-global, not per-view**: `Display.h:182-185,221-223` (`enableLetterBox`, `m_letterBoxEnabled`, fade); consumed at `W3DView.cpp:344` via `TheDisplay->isLetterBoxed()`. Multi-seat: make `enableLetterBox` a no-op when >1 seat bound. |
| V7 | Insertion point for seat stream messages? | WP2 | `GameClient::update` — `GameClient.cpp:610-611` (`TheMouse->UPDATE(); TheMouse->createStreamMessages();`). Insert `TheSeatManager->createStreamMessages()` immediately after :611. Keyboard equivalent at :599-600. |
| V8 | `ControlBarResizer` arbitrary scale? | WP8 | **No** — INI-driven two-state only (`ControlBarResizer.h:63-89`: per-named-window `default` and `alt` size/pos). WP8 writes its own recursive uniform-scale helper; the resizer is precedent, not a tool. |
| V9 | House color getter? | WP3 | `Color Player::getPlayerColor() const` (`Player.h:251`, returns `m_color`). |
| V10 | `GameMessage` ctor safe in shell? | WP2 | **Yes in practice**: the shell already constructs GameMessages today; `getLocalPlayer()` asserts non-null (`PlayerList.h:120`) and `setLocalPlayer` handles first-call null (`PlayerList.cpp:315-319`). Rule: never construct a `GameMessage` before `ThePlayerList` init. |

## 3. Decision log (append-only; one line each: date, WP, decision, why)

- 2026-07-02 · plan · Seat ≠ Viewport ≠ Player abstractions; shared-screen & co-op internal-only (user)
- 2026-07-02 · plan · AI takeover on leave; cursor = house color; lobby+in-game controller scope only (user)
- 2026-07-02 · WP8 · No new GUIs — classic ControlBar instanced; top bars vertically mirrored; 8P = 4+4 quarter-width (user)
- 2026-07-02 · WP9 · LAN must keep working; `CommandTransport` seam formalized around `TheCommandList` (user)
- 2026-07-02 · test · User has real controllers; 3 pads = test ceiling (4–8 seats add no new code paths — layout only, cover with fake seats) (user)
- 2026-07-02 · all · Pre-flight V1–V10 resolved by code reading (see §2); WP9 gains the skirmish-script-stash fix candidate from V5
- 2026-07-22 · WP0 · Seat 0 = keyboard/mouse (m_deviceId = -1), always BOUND; gamepads claim seats 1..7 (findFreeSeat starts at 1). Per plan2 WP0 + conventions §Subsystems.
- 2026-07-22 · WP0 · SeatManager registered as a SubsystemInterface in GeneralsMD GameEngine.cpp immediately before TheGameClient (input+InGameUI live inside TheGameClient, so "after input / before InGameUI" both resolve to just-before-TheGameClient). reset() keeps device bindings, clears per-match state.
- 2026-07-22 · WP1 · **Deviation from plan2 WP1 step 4 ("delete gamepad injection"):** kept the legacy gamepad→mouse/keyboard injection but GATED it behind the splitscreen dev flag. Flag OFF ⇒ first pad drives the OS mouse exactly as today (zero single-player regression, satisfies ground rule 2 + plan.md §3 "playable at every commit"). Flag ON ⇒ pads route to seats via setSeatInput instead. Reversible; injection is deleted for good once WP2 (createStreamMessages) makes seat-0 controller play work through the message stream.
- 2026-07-22 · WP1 · Splitscreen gate lives on SeatManager (`m_enabled`, isSplitscreenEnabled) rather than reading TheGlobalData from Core code — avoids Core depending on a GeneralsMD-only GlobalData field (Generals build shares Core). The `-splitscreendev` command-line flag + GlobalData wiring that flips it is still TODO (WP1 remainder).
- 2026-07-22 · env · Build verified compiling via existing `build/win32` (Ninja Multi-Config, MSVC 2022 through Enter-VsDevShell). User confirmed: keep using `build/win32`. Preset alt (`win32-vcpkg-debug`) not needed.
- 2026-07-23 · WP3 · Per-seat software cursors, additive-only. (A) `SeatManager::createStreamMessages` now integrates each bound device-seat's (1..7) own virtual cursor from its left stick: fixed 15px/frame step (x0.35 when left trigger > 0.5), center-seeded via m_cursorInit, clamped to [0..TheDisplay width/height], writes m_cursor.pos + visible=TRUE. Does NOT touch TheMouse and appends NO GameMessages (translators seat-unaware until WP5). (B) New Core/GameEngineDevice W3DDevice files `W3DSeatCursorRenderer.{h,cpp}` (CMake SDL3 block) draw each visible seat cursor as a small solid tinted right-triangle arrow (tip = hot-spot) + 1px black outline, via `TheDisplay->drawFillRect/drawLine` — chose Display primitives over a hand-rolled Render2DClass for robustness (they set up shader/coord-range; still Add_Quad-tinted per V1, no shader). No cursor art assets added. (C) Tint = `getNthPlayer(m_playerIndex)->getPlayerColor()|0xFF000000` when the seat has a player, else an 8-color `fixedSeatPalette`. (D) Hooked in `W3DDisplay::draw` immediately after `TheMouse->DRAW()` (so cursors sit on top of UI+mouse), inside `#if RTS_SDL3_ENABLE`; whole renderer gated on `TheSeatManager && isSplitscreenEnabled()`. (E) OS-cursor hiding for a pad-driven seat 0 deliberately skipped (WP5 concern; WP3 additive only). Compile-verified (MSVC 2022, x86, build/win32 Debug): SeatManager.cpp.obj (z_gameengine), W3DSeatCursorRenderer.cpp.obj + W3DDisplay.cpp.obj (z_gameenginedevice) all built clean; W3DDisplay's only warning (C4018 @2043) is pre-existing and unrelated.
- 2026-07-23 · scope · **User scoping decision:** goal is mkb = player 1, one controller = player 2, shared screen. Build **WP3 → WP4 → WP5** (the player-2 core) then **WP6 (split viewports)**. **Defer WP7 (per-viewport fog/shroud/ghosts) and WP8 (per-seat control bars) and WP9 (lobby)** for now — split viewports without per-player fog is acceptable for this pass. Each WP delegated to a subagent, run sequentially in the working tree (WP0-2 uncommitted → no worktree isolation; WP5 depends on WP3+WP4).
- 2026-07-23 · WP2 · **Reverted the shared-mouse experiment** per user feedback: `SeatManager::createStreamMessages` is now empty and the SDL3 loop no longer `continue`s past legacy injection for a bound pad. The primary pad keeps the FULL legacy button mapping (all buttons restored). `GameMessage::m_seatIndex` tag infrastructure kept. The controller is player-1-via-mouse until WP5 moves it to its own seat/player.
- 2026-07-22 · WP2 · `createStreamMessages` makes the active pad seat drive the shared OS cursor (`TheMouse->setPosition` + a new `SDL3Mouse::setPosition` that warps the OS cursor) and emits seat-tagged raw mouse messages. Chosen over pure message emission because the visible cursor is the OS hardware cursor (engine `setPosition` alone wouldn't move it), so a pad wouldn't be *visibly* playable pre-WP3 otherwise. Reconciles WP2 acceptance ("indistinguishable from mouse events") with WP0's seat-0=kb/mouse model: a pad possesses the shared pointer as the local player until WP5 splits control per seat.
- 2026-07-22 · WP1 · Seat debug overlay reuses the existing `DebugDisplay` callback slot (`setDebugDisplayCallback`), auto-installed by `SeatManager::update()` only when the slot is free (won't stomp an active F-key debug display). Avoided new `MSG_META`/`CommandMap.ini` data changes. Overlay is RTS_DEBUG-only (that facility is), which is fine for dev.
- 2026-07-22 · WP1 · `-splitscreendev` gate lives on GlobalData (GeneralsMD only) and is pushed into SeatManager; SeatManager itself never reads GlobalData (keeps Core from depending on a GeneralsMD-only field, so the shared Core file stays clean for a Generals SDL3 build too).
- 2026-07-24 · WP9/WP7 · **Deterministic seat→player binding (fixes "second viewport not assigned to the second army").** Two-part fix, one root cause: the seat's player index (which the viewport's `setRenderPlayerIndex` reads) was chosen by an "i-th non-local playable player" heuristic that ignored which lobby slot the controller joined, so on any map with >1 AI the viewport bound to the wrong army. (A) **Lobby claim (SkirmishGameOptionsMenu.cpp):** the default skirmish makes slot 1 an *Easy AI* (not open), so the old `slot->isOpen()`-only claim never matched → player 2 never appeared. Now a bound controller seat claims the first slot that is open **or** a default AI (it replaces that AI), skipping human slots and slots another seat already took; sets it `SLOT_EASY_AI` titled "Controller N" and records `m_lobbySlot`. (B) **In-game takeover (SeatManager.cpp):** GameLogic creates slot k as the player named `player%d` (k). The takeover now looks up `findPlayerWithNameKey("player<m_lobbySlot>")` and binds/AI→human that exact player; the old heuristic remains only as a fallback for lobby-less dev launches. `m_lobbySlot` survives the match-start `reset()` (clearMatchState keeps it). Net: the army the controller joins as == the army it commands == the army its viewport renders. Compile-verified (build/win32 Debug, MSVC x86): SeatManager.cpp + SkirmishGameOptionsMenu.cpp built, generalszh.exe linked. Needs runtime confirm with a controller.
- 2026-07-24 · WP5 · **FOUND THE REAL SELECTION BUG via overlay instrumentation (routing was already fine).** Added live diagnostics (ROUTE lastClickSeat/lastActiveSeat, per-seat msgs, SHROUD fills/lastPlayer) - user screenshots proved my prior assumptions WRONG: the controller's cooked click DOES keep seat tag 1 (lastClickSeat=1, lastActiveSeat=1) and the per-view shroud DOES run (fills=6768). So neither "lost tag" nor "shroud never runs" was real. The actual selection bug: `InGameUI::selectDrawable(draw)`, `deselectDrawable(draw)`, and `deselectAllDrawables(postMsg)` - the legacy no-arg accessors SelectionXlat calls - HARD-CODED seat 0 (`selectDrawable(draw,0)` etc.) instead of `m_activeSeat`, even though sibling accessors (getSelectCount/getAllSelectedDrawables) already use m_activeSeat. So a correctly-tagged seat-1 click still mutated seat 0's (player 1's) selection: player 2's picks landed in player 1's context, and player 2's empty-click deselected player 1. Fixed all three to `m_activeSeat` (== 0 for mouse, so single-player identical). NOTE: build-placement legacy forwards (placeBuildAvailable/setPlacementStart/End/getPlacementPoints at InGameUI ~3343/3507/3534/3569) have the same hard-coded-0 pattern - deferred until player-2 building. Black second viewport still open: shroud runs but view black; added `nonLocalClear` diag (revealed-cell count for the 2nd player's fog) to decide "player 3 has no vision" vs "camera/upload". Build clean (VS2026).
- 2026-07-24 · WP5 · **Fixed passthrough clicks (controller moved player 1's selected unit / off-screen).** Root cause: `GameMessage` ctor inherited `m_playerIndex` from `TheSeatActingPlayerOverride` but HARD-CODED `m_seatIndex = 0`. So a seat's raw click, once translated into a cooked `MSG_MOUSE_LEFT_CLICK` (and then `MSG_DO_MOVETO`), lost its seat tag; the next translator processed it with no override → it acted on the primary local player in viewport 0 (moved player 1's unit, destination through the wrong view = "behind the second instance"). Fix: added a parallel `TheSeatActingSeatOverride` global that `propagateMessages` sets to the acting `seatIdx` alongside the player/view scoping; the ctor now stamps `m_seatIndex` from it. Derived messages inherit the seat → re-establish the override when re-processed → route to the seat's player+view. Identical for seat 0 / mouse / LAN (override -1 => seatIndex 0). Build+link clean (VS2026).
- 2026-07-24 · WP7 · **Fog root cause found: shroud bind used a STALE flag (fix: live override).** Full instrumentation disproved both prior candidates - `bindSec=311` (right view DOES bind the secondary sometimes) and `aimFound=1 aim=cam=(1362,1966)` (camera IS correctly on player 2's base, so init/camera is fine). The tell was `bindPrim=2280` vs `bindSec=311` (~7:1): the terrain binds the shroud texture ~7x/frame but only ~1 is secondary, so the right view's *visible* terrain pass samples the PRIMARY texture - `m_useSecondaryDst` (set once in prepareShroudForView) is stale by the time the material pass (`W3DShroudMaterialPassClass::Install_Materials`) actually binds. Fix: `W3DShroud::getShroudTexture()` now decides from the LIVE `rts::getObservedOrLocalPlayerIndex_Safe()` (!= local player) - the same per-view render override that makes object visibility work - instead of the pre-set flag. So the terrain hands out the secondary fog texture whenever a non-local player is being rendered, regardless of when in the frame the bind happens. Build clean (VS2026). Needs user confirm; expect bindSec to rise sharply relative to bindPrim.
- 2026-07-24 · WP7 · **Fog research round 2 (copy ruled out; instrumenting bind + camera).** `2ndShroudRenders=373` + `nonLocalClear=3900` prove the secondary fog texture IS written with player 2's data. Researched the copy: `_Copy_DX8_Rects` = `IDirect3DDevice8::CopyRects` from a system-memory src (`CreateImageSurface`) to the vidmem dst = IMMEDIATE/synchronous, so dst2 gets player 2's fog before seat 0 overwrites the src - the shared-src theory is WRONG too. So the black right view is now down to two instrumented candidates: (1) the terrain binds the PRIMARY texture for both views (bind happens when m_useSecondaryDst already reset) - added `g_dbgShroudBindPrimary/Secondary` counters in `getShroudTexture()`; (2) the 2nd viewport's CAMERA isn't on player 2's base - the `lookAt` runs ONCE at view creation and only `if (aim.found)`, so if player 2's units haven't spawned that frame the camera stays at its default (unexplored => black); the shared-texture build masked this via player 1's fog. Added `g_dbgSeat1AimFound/Aim(X,Y)/Cam(X,Y)`. Overlay now shows `bindPrim=/bindSec=` and `SEAT1CAM aimFound=/aim=/cam=`. Also relabeled army as P<seat+1>(engineIdxN). NEXT (pending user screenshot): bindSec≈0 => fix bind timing; aimFound=0 or cam far from aim => re-aim the camera each frame until the base exists.
- 2026-07-24 · WP9 · Skirmish lobby: claim fires (`claims=1 lob=1 st=2`) but the slot shows as an "Easy AI" dropdown; user wants it shown as "<player1 name> (2)" (a joined player, dropdown gone). Needs the claimed slot presented as a player row, not an AI difficulty combo - deferred behind the fog fix.
- 2026-07-24 · WP7 · **Second-shroud-texture STILL shows black right view (diagnosing).** User confirmed with the second texture the right view is now fully black (was only visible before because the LOCAL player had scouted player 2's base). This means both views still sample the PRIMARY (local) shroud texture: left = local base visible, right = player 3's base which the local player hasn't scouted = black. Since object-visibility per-view works (render is immediate in drawView, override active), the terrain SHOULD bind the secondary during the second view - so either the secondary texture isn't written, or `_Copy_DX8_Rects(m_pSrcTexture->dst2)` is deferred and reads the final (player-2) src (the src texture is shared between both dst copies). Added `g_dbgSecondaryShroudRenders` (incremented when render() targets the secondary) to disambiguate: >0 = texture written (=> shared-src/copy-deferral is the culprit, need a second SRC texture too); 0 = the secondary path never runs. Also: reverted the setMouseCursor seat-scoping (it froze player 1's cursor animation); relabeled overlay army as `P<seat+1>(engineIdxN)` since engine indices reserve 0=neutral/1=civilian so the mouse is engineIdx2 and the controller engineIdx3 (= human "Player 2").
- 2026-07-24 · WP7 · **Per-view fog: SECOND shroud texture (the actual fix).** User confirmed the earlier per-view attempt didn't work - the right view only showed what player 1 had discovered. Root cause (confirmed by reading W3DShroud): DO_FOG_INTERPOLATION is OFF, so no fade; render() `_Copy_DX8_Rects(m_pSrcTexture -> m_pDstTexture)` into ONE dst texture that BOTH viewports sample - so re-filling+re-uploading per view just made the last write (seat 0 = local, drawn last) win for both. Fix: `W3DShroud` now owns a second dst texture `m_pDstTexture2` (+ `m_useSecondaryDst`/`m_clearDstTexture2`); `setActiveShroudTarget(secondary)` lazily allocates it and switches which texture render() writes and getShroudTexture() returns; `W3DDisplay::prepareShroudForView` sets secondary when the view's render player != local player. Separate textures + immediate sysmem->vidmem CopyRects => the two viewports' fog no longer overwrite. LIMITATION: one secondary texture = supports 1 local + 1 controller (2-player). 3+ splitscreen needs an array of dst textures. Build clean (VS2026). Needs user confirm.
- 2026-07-24 · WP5/WP3 · Cursor flicker fix + lobby debug. (A) `InGameUI::setMouseCursor` now records the cursor type on the acting SEAT's software cursor when m_activeSeat>0 instead of stomping the shared OS mouse cursor (fixes player 1's cursor reverting to default whenever the controller moved). (B) Added lobby-claim diagnostics to the overlay (LOBBY claims=/lastSlot=, per-seat lob=m_lobbySlot) to diagnose why player 2 isn't visually placed in the skirmish setup - the claim sets slot i to SLOT_EASY_AI titled "Controller i", which looks identical to the default AI, so the "visual replacement" likely needs a distinct slot state (pending the user's skirmish-setup screenshot of claims=/lob=).
- 2026-07-24 · WP7 · **(superseded) Per-view fog via single-texture re-upload (did NOT work).** The shroud is one global texture uploaded once per frame for the primary player (W3DDisplay::draw ~1908), so every viewport sampled player 1's fog. Added `PartitionManager::refreshShroudForRenderPlayer()` (fills the display shroud src for `rts::getObservedOrLocalPlayerIndex_Safe()` = the per-view override; no radar/COI side effects), a `Display::prepareShroudForView(View*)` virtual (base no-op) that `Display::drawViews` calls per view **only when multiView** (>1 view, so single-player is provably untouched), and `W3DDisplay::prepareShroudForView` override that refills the src for that view's player then re-uploads via `getShroud()->render(view camera)` before the view draws. Net: each viewport shows its OWN player's fog (right view now shows player 3's revealed base instead of black). Cost is ~1 extra full shroud refill+upload per extra view per frame - fine for dev. NOTE: `PartitionManager.h` include in W3DDisplay.cpp was `#ifdef DUMP_PERF_STATS`-only; made it unconditional. Build+link clean (VS2026).
- 2026-07-24 · WP7 · **(superseded) Diagnosed the black second viewport.** Overlay confirmed viewports now tile correctly (seat1 vp=(400,0 400x600)). The right view is black because the fog-of-war is a SINGLE global texture: `PartitionManager::refreshShroudForLocalPlayer` pushes `rts::getObservedOrLocalPlayer()`'s shroud into `TheDisplay`/`W3DShroud` (one src texture, uploaded once per frame by `W3DShroud::render`), and it ignores the per-view render-player override, so every viewport samples player 1's fog → player 3's area is black until player 1 explores it. Per-player shroud DATA exists (`getShroudStatusForPlayer(idx,...)`), so the user's "all players are local" point holds; the fix is per-view shroud (re-fill W3DShroud src for each view's player before its terrain draw, or give local seats shared/allied vision) - real WP7 render work, deferred pending a decision rather than hacking the render blind.
- 2026-07-24 · WP9 · **Committed the strict 1:1:1:1 model (user directive: "army slot = viewport slot = player slot = control slot, all linked; the 2-player-in-1-view work gone").** There was no shared-screen/co-op code to delete - it was a plan-only concept (plan §1/§5 "two seats may share one viewport") superseded by real splitscreen and never implemented. So the change is to make the linkage explicit and drop indirection: `SeatManager::createStreamMessages` takeover now binds **seat i directly to game slot i** = the player GameLogic named `player<i>` (removed the `m_lobbySlot` lookup AND the "i-th playable player" heuristic fallback). Seat 0 = kbd/mouse = slot 0 = local player. The skirmish lobby already places a player in game slot i for each bound seat i (seat i -> slot i), so `player<i>` exists at takeover time. Result: control(device) -> seat i -> game slot i -> army(player#i+1) -> viewport i, one index end to end. Also enriched the seat debug overlay with a second per-seat line - `gameSlot=i  army=player#N [=LOCAL!]  vp=(x,y WxH)|NONE` - so a single glance shows which link is broken (`=LOCAL!` = the "bound to player 1" bug; `vp=NONE` = no viewport made). Build+link clean.
- 2026-07-24 · WP6/WP7 · **Choke-point view swap replaces per-call-site view patching (user architectural steer: "unsingleton TheTacticalView, don't write code around it").** Root cause of "clicks on the right viewport register on the first screen", "lasso limited to one viewport", and the general "linked to player 1" feel: `TheTacticalView` is a hardcoded singleton referenced at dozens of sites (screenToTerrain for move/attack destinations, pickDrawable, iterateDrawablesInRegion, worldToScreen, camera look-at). We had only patched a handful to `getCommandActingView()`; the command-*destination* `screenToTerrain` (CommandXlat 3903/3933/3974/4000) and the drag-select `iterateDrawablesInRegion` (SelectionXlat 612) still used the left viewport, so a controller's clicks/lassos resolved in seat 0's view. True unsingletoning (threading View* through hundreds of sites) is a multi-KLOC refactor that breaks every intermediate build; instead **`MessageStream::propagateMessages` now repoints the `TheTacticalView` global at the acting seat's `m_view` for the duration of each seat>0 `translateGameMessage`, then restores it** (right beside the existing acting-player + active-seat scoping). Every `TheTacticalView->` reference the translators reach now resolves in the seat's viewport with zero per-site code and no missed sites. Gated on `seatIdx>0 && RTS_SDL3_ENABLE` so seat 0 / mouse / LAN (seatIndex 0) are byte-identical. The scattered `getCommandActingView()` calls are now redundant belt-and-suspenders (they return the same view during the swap) - safe to strip in a later cleanup. Build+link clean (MessageStream/CommandXlat/SelectionXlat, generalszh.exe).
- 2026-07-24 · WP9 · **Confirmed the slot→player→index model by reading GameLogic::startNewGame + PlayerList::newGame:** GameLogic creates one side per occupied slot named `player<slot>`; PlayerList reserves index 0 for Neutral (m_playerCount starts at 1), so **player index = slot index + 1**. Skirmish hardcodes slot 0 = the local kbd/mouse human (index 1, the `multiplayerIsLocal` side). A controller on seat N → slot N → index N+1. So seat 1 → slot 1 → "player1" → index 2 (the *second* army), and it never collides with the mouse's index-1 player (the "player1" name is 0-based and only *sounds* like Player 1). The controller becomes human via setPlayerType *after* newGame, so it never disturbs local-player selection. Net: seat→slot→index is now internally consistent for render, selection, and command.
- 2026-07-24 · WP0 · **Seat packing (fixes controllers drifting to seat 2/3).** `findFreeSeat` now returns the lowest seat that is UNBOUND *or* SEAT_DEVICE_LOST (was UNBOUND only), and `bindSeatToDevice` `reset()`s the seat before binding a fresh device. Reason: during dev testing a replugged pad gets a new SDL instance id each time, so old seats stuck at DEVICE_LOST pushed the live controller to seat 3 (the `dev=3` the user saw), which then took slot 3 → wrong army. Same-id reconnect is still handled first (getSeatForDevice), so reclaiming a lost seat here is safe and keeps controllers packed at low indices → predictable seat i → slot i → index i+1.
- 2026-07-24 · WP7 · Control-routing fix integrated (from subagent): new `Object::isControlledByPlayer(player)`; the order gate `InGameUI::areSelectedObjectsControllable` + all command/selection-cycle sites in CommandXlat/SelectionXlat now test the acting seat's player (`getCommandActingPlayer()`) instead of `isLocallyControlled()`, so a controller can *command* (not just select) its taken-over army. Behavior-identical for mouse/single-player (override -1 ⇒ acting player == local player). Scaling-hack per-viewport control-bar attempt reverted to the clean shared full-screen bar; Escape-frees-cursors retained. Per-viewport control bar remains a deferred rendering spike (no clean draw seam — bar windows draw in the global repaint).
- 2026-07-22 · WP0 · **VC6 gating (user directive):** the whole splitscreen feature depends on SDL3 (multiple mice / 8 controllers), so the seat model is useless on VC6. Corrects plan2 WP0's "device-independent, registered unconditionally": SeatManager/SeatInput are still SDL-free C++, but are compiled **only into SDL3 builds** — cmake entries gated behind `RTS_BUILD_OPTION_SDL3` (forced OFF for VC6), and the `GameEngine.cpp` include + `initSubsystem(TheSeatManager,...)` wrapped in `#if RTS_SDL3_ENABLE` (the same macro WinMain uses to pick SDL3GameEngine). VC6 compiles zero seat code. Verified win32 (SDL3 ON) still builds with the gate.
- 2026-07-25 · WP7 · ~~**ROOT CAUSE of the black 2nd viewport: the mid-frame secondary shroud texture.**~~ **[SUPERSEDED — this diagnosis was WRONG; see the next entry. The single-shared-texture change it introduced was kept, because it is simpler and provably correct, but it did not fix the black viewport.]** Fix: go back to ONE shared texture, refilled per view. The isolation test (force `dst2` all-CLEAR → right view STILL black) was the decider: the terrain *did* bind `dst2` (`bindSec≈1/frame`) but `dst2`'s sampled content was never valid, so the bug was the texture itself, not the bind/UV/camera (all previously ruled OK). `m_pDstTexture2` is a `POOL_DEFAULT` vidmem texture **created mid-frame** inside `setActiveShroudTarget` (called from `prepareShroudForView` during render); the primary `m_pDstTexture` is created at device-init in `ReAcquireResources` and is a proven `CopyRects` destination, but the mid-frame one never received the sysmem→vidmem `_Copy_DX8_Rects` correctly → always shrouded/black. Contrast: per-view *object* visibility always worked because it reads the render-player override **directly** during the same synchronous `doRender`, never via a texture. Fix (single shared texture): `W3DDisplay::prepareShroudForView` now always `setActiveShroudTarget(FALSE)` and refills+re-uploads the ONE primary texture with this view's render-player fog immediately before that view's synchronous terrain draw; `W3DShroud::getShroudTexture()` always returns `m_pDstTexture`. Ordering is safe because the scene render is synchronous per view (proven by object visibility) and `CopyRects` sysmem→vidmem is immediate: right view (override=player2) refills+draws, then left/seat-0 view refills back to local and draws last (leaving primary in local state for radar/UI). `m_pDstTexture2`/`setActiveShroudTarget(TRUE)` are now dormant (never invoked); `g_dbgForceSecondaryClear` reset to 0. Needed a clean reconfigure first (pinned SDK 10.0.26100.0 was uninstalled; only 10.0.28000.0 remains — deleted CMakeCache/CMakeFiles, reconfigured win32 preset, PCH rebuilt against 28000). Build+link clean (generalszh.exe). **Needs in-game confirm: right viewport should now show player 2's OWN vision, independent of what player 1 has scouted.**
- 2026-07-25 · WP7 · **REAL ROOT CAUSE of the black 2nd viewport: the terrain draw window is GLOBAL, not per-view. It was never a fog bug.** `HeightMapRenderObjClass` only builds geometry for a sliding window of tiles around one centre, and that centre is global state on `TheTerrainRenderObject`, set by `W3DView::updateTerrain()` (W3DView.cpp:3695 — `setTerrainDrawSize` + `updateCenter(m_3DCamera, &cameraPivot, it)`), which is only called from camera-update paths (W3DView.cpp:854 `setCameraTransform`, :1398 `doesNeedFullUpdate()`). With two viewports, **whichever view moved its camera last owns the window for every viewport** — the right viewport was drawing player 1's tiles, so at player 2's base there was no terrain geometry at all → black. Decisive evidence was the user's 5 labelled states: right view black when P1's camera is elsewhere (1), lit when P1 pans over (2), both fine when P1 is there (3), and **P2's buildings floating on totally black ground** once P1 pans away again (4/5) — objects honour the per-view render override, terrain geometry doesn't exist to render. All fog probes were already correct before the fix: `terrainOverride=3` (override live at the actual terrain shroud bind), `srcAtBase`/`TEXlvl=255` (shroud texture genuinely says "lit" at P2's base), `LOGICvisAtBase=0` (`CELLSHROUD_CLEAR` — P2 really has vision), `objRenderPlayer=3`, `EYEht=328` / `aim=(1063,428)` (right camera correctly perched over P2's base). Fix: call `updateTerrain()` in `W3DView::draw()` immediately before `doRender`, gated on multi-view via the **public** `TheDisplay->getNextView( TheDisplay->getFirstView() )` (`View::getNextView()` itself is protected — that was the first compile error), so each viewport re-centres the terrain on its own camera. Cheap and safe: `updateCenter` early-outs when the draw origin hasn't moved, and single-view behaviour is unchanged by the gate. Also note the earlier `objRenderPlayer` probe was itself buggy — every view wrote it, so it reported the *last* (local) view; fixed by only capturing when the render player differs from the local player. Build+link clean (generalszh.exe). **Needs in-game confirm of states 1 and 4: lit terrain UNDER player 2's buildings in the right viewport.** Broader lesson for the rest of splitscreen: audit **all global render state a view mutates** (LOD/detail centres, water plane, anything keyed off "the" camera), not just visibility.

- 2026-07-26 · WP7 · **Render-leak probe (stop guessing) + the general owner-filter rule.** Three sessions guessed at the remaining leaks and were wrong every time, so this pass builds the instrument first. New `Core/GameEngine/{Include,Source}/Common/RenderLeakProbe.*`: point the mouse at the leaking thing; each frame the mouse pixel is the target, the viewport containing it becomes "probed", and every render decision about an object that projects within 70px of that pixel appends a row to the seat overlay - `<dist>px <path> <name> own=P<n> ss=<n> ghost=P<n> -> DREW…/SKIP…`. `path` names the call site (`main`/`occluder`/`occludee`/`nonoccl`/`stencil`/`transluc`/`occluded`/`specific`/`particle`), so a leak is read off one line instead of inferred. Wired at `Display::drawViews` (beginFrame/beginView/endView), `RTS3DScene::renderOneObject` (a `callPath` label + a row at every early return and at the draw), the "not visible (Visibility_Check)" negative case, and the particle cull. Cost is one branch per object outside the probed viewport. **Then the fix, as one rule rather than four patches:** the leaks all came from consumers of `RenderObjClass::Is_Really_Visible()` - the object draw, its shadow, its occlusion silhouette, the translucent/stencil passes - while only the object draw had a per-view shroud test. So `RTS3DScene::Visibility_Check` (which runs per view) now applies the owner filter once, at the source of that bit: new `seatOwnerFilterHidesObject()` clears visibility for (a) real objects this viewport's player cannot see - they are legitimately in the shared scene because another seat still sees them - and (b) ghost snapshots owned by another seat. That kills the leaked buildings AND their shadows, which is what the dark building-shaped blobs were. Uses a new side-effect-free `Object::peekShroudedStatus()` because `getShroudedStatus()` can take a ghost snapshot, which adds/removes render objects mid-iteration of the scene's render list. Smoke: the particle cull only tested systems with an *attached object*, so explosion/debris/weapon smoke and anything whose owner had already died (`attachedID == INVALID_ID`) drew in every viewport - it now falls back to the system's own world position against this viewport's fog. Ghosts: `m_sceneSnapshotPlayer` was left unset on the `setLocalPlayerIndex` and `loadPostProcess` add-to-scene paths, so those snapshots had no owner and passed every viewport's filter; both now record it. Cursor: seat 0's pointer is the OS cursor, drawn by the window manager over the whole window - confining it depends on the OS honouring `SDL_SetWindowMouseRect`, which the game cannot guarantee. So while the screen is split the OS cursor is hidden and seat 0 draws a software cursor like every other seat (`SeatManager::setSeat0UsesSoftwareCursor`), mirroring `m_currMouse.pos`, which the engine already clamps to seat 0's viewport - it cannot escape regardless of what the OS does with the physical pointer. Restored on the quit menu and outside splitscreen. Build+link clean (VS18 x86 Debug, generalszh.exe). **Needs in-game confirm; the probe rows are the check.** Known corner case left open: when a non-seat-0 player is the LAST to fog a building, its snapshot owns the scene slot and seat 0 sees nothing there rather than its own remembered building - fixing that needs one snapshot per seat in the scene, which needs a `DrawableInfo` per owner (all snapshots currently share the ghost's single one).

- 2026-07-26 · WP1/WP2 · **Input unified: the seat layer owns every pad.** Pad handling was two parallel systems - the legacy `injectLegacyMouseKeyboard` path (driven by SDL3Input's own `m_primaryDevice`) and the seat path - each applying its own admission rules to the same device list. A per-seat control bar is not clickable until one layer owns every device, so they are now one: every pad goes through new `SeatManager::routeDeviceInput(deviceId, state)` each frame regardless of the splitscreen flag, and the returned seat is the ONLY thing SDL3Input branches on (`>0` seat drives its own cursor/messages and the backend injects nothing; `0` = run the legacy injection, which is now simply *what seat 0 does with a pad*; `-1` = another pad already acts for seat 0, idle until it joins). `m_seat0DeviceId` keeps exactly one pad in the seat-0 role so two cannot fight over the single OS pointer; `onDeviceRemoved` releases it on unplug. `SDL3InputManager::m_primaryDevice` deleted - device roles are the seat layer's call, remade per frame. Flag OFF is behaviourally identical to before (first pad drives the mouse). Build+link clean.
- 2026-07-26 · WP8 · **ControlBar instancing foundation landed; the sweep is NOT done.** What is in: (1) `GameWindowManager::winFindChildById(root,id)` - the strict subtree lookup V2 said was mandatory. `winGetWindowFromId` walks the given window's children *and then its trailing `m_next` siblings*, which is harmless with one bar but silently resolves to another instance's identically-named widget once bars are siblings. (2) `ControlBar` gained per-instance identity - `m_seatIndex`, `m_barPlayer`, `m_barRootWindow` - plus `findBarWindow(name)`/`findBarWindowById(id)`, which scope strictly to this instance's tree; instance 0 keeps a global-lookup fallback (so windows outside the `ControlBarParent` subtree - `GeneralsExpPoints.wnd`, the shortcut parent - still resolve exactly as before), while a miss on any other instance stays a miss rather than stealing seat 0's widget. All 34 `winGetWindowFromId(nullptr, …)` sites in ControlBar.cpp converted; the two that must stay global (the CP_MASTER root lookup and findBarWindowById's own fallback) are marked. (3) `getBarPlayer()` + 45 `ThePlayerList->getLocalPlayer()` sites in ControlBar/ControlBarCommand/ControlBarCommandProcessing/ControlBarStructureInventory routed through it. **It deliberately falls back to `getLocalPlayer()`, not `getObservedOrLocalPlayer()`** - the conversion has to be provably byte-identical for one seat, and observer mode is already handled by `m_observedPlayer`. (4) `ControlBarInstances` registry with `fromWindow()`, which traces a firing `GameWindow` back up to the bar that owns it - the only way a callback can know which seat (and army) a click belongs to. Instance 0 registers itself at `ControlBar::init`. **Still open (the bulk of WP8):** actually creating a second `ControlBar.wnd` instance per viewport (Spike C: bottom + mirrored top, both clickable), the geometry/scale/mirror pass, the ~40 remaining `"ControlBar.wnd:*"` lookups in ControlBarCommand/Observer/Scheme/Beacon/OCLTimer/callbacks/InGameUI/CommandXlat/W3DControlBar, routing the callbacks through `fromWindow()`, iterating instances in the update loop, per-seat window hit-testing, and per-instance radar. Everything landed is behaviour-identical for a single bar; generalszh.exe builds and links clean.
- 2026-07-26 · build · Note: the **Generals (non-ZH) `generalsv.exe` target does not build** on this branch and did not before this session either - `Core/.../SeatManager.cpp` uses `GameMessage::friend_setSeatIndex`, which WP2 added only to `GeneralsMD/.../MessageStream.h`. Consistent with plan2 ground rule 1 (work only in `GeneralsMD/` and `Core/`; the Generals port is a later pass), but it means Core is no longer Generals-clean and the port pass will have to add the seat tag there.

- 2026-07-26 · WP7 · **Shadows: the probe found it, and it was the same bug as the terrain window.** The `SHADOWvol/SHADOWdecal/SHADOWpass` overlay lines settled it: every pass runs in every view (`SHADOWpass=3` throughout) and ~5 volumes clear the visibility gate per view, so shadows WERE being rendered - they just landed nowhere visible. Root cause: **both full-screen passes that finish a shadow ask the `TheTacticalView` global for the rect to cover** - `W3DVolumetricShadowManager::renderStencilShadows` (the stencil darkening quad) and `renderStenciledPlayerColor` in W3DScene (behind-building silhouettes). That global is seat 0's view, so every viewport painted its result over seat 0's rectangle and nowhere else. Fix: new `rts::setRenderViewRect/getRenderViewRect` (set in `Display::drawViews` beside the render-player override; unset = full display), used by both. Also both quads hardcoded `0` for their top edge, which only looked right for a view starting at screen top. Two earlier diagnostic dead ends worth recording: (1) the first shadow probe instrumented only the **decal** list, whose `0/11`-per-view reading is *normal frustum culling*, not a fault - unit shadows are stencil **volumes**, a separate list; (2) the `SEATCURSOR` line initially reported the LAST seat drawn, so a healthy `seat7 SCCPointer.tga 32x32` stood in for seat 0's broken cursor - it now reports the first seat drawn.
- 2026-07-26 · WP3 · Seat-0 cursor: the magenta block was the SELECTING cursor resolving to non-cursor art. `Mouse.ini` describes a cursor as EITHER a mapped image (atlas entry, self-describing) or a raw texture name, and the seat renderer only implemented the texture path, so an atlas entry loaded the whole page and drew it at 0..1 UVs. Now prefers the mapped image (as `W3DMouse::initPolygonAssets` does) and rejects art larger than 64px - the game's own cursors are 32x32, so anything bigger is a placeholder or an atlas page. Rejected art falls back to the arrow.
- 2026-07-26 · WP8 · **Per-viewport ControlBar landed (structure complete, runtime unverified).** Each active seat gets its own `ControlBar`: own `ControlBar.wnd` layout, own window cache, own player, docked into its own viewport. Key pieces: (1) `init()` split - INI loading (command buttons/sets/scheme) describes the *game* and stays shared; new `initInstanceWindows()` is the per-instance part, and `initAsSeatInstance(seat, player, shareFrom)` borrows the shared data with `m_sharesGameData` so a teardown cannot free what other bars use. (2) **A bar is several window trees, not one** - `ControlBar.wnd` creates the command bar, right HUD and radar as separate roots, plus the science layout - which is why the first dock attempt left the satellite cameo behind. `findBarWindowById` now searches every root the instance owns; instance 0 keeps a global fallback, other instances stay strict. (3) `ControlBarInstances::{syncToSeats,destroySeatInstances,updateAll,fromWindow}` - creation/re-dock/teardown driven from `updateSeatViewports`, updates driven from `InGameUI::update`, and callbacks routed by walking the firing `GameWindow` up to its owning instance (`fromWindow` returns `TheControlBar` when there is one bar, so single-seat is unchanged). (4) `dockToRect` maps the whole display rect onto the viewport rect so every piece keeps its relative screen position, converting from the scale in force (so returning to full-screen restores the authored layout exactly); `ControlBarScheme` paints straight to the display rather than through the window system, so it is given the same scale and its `BackgroundMarker` anchor is scaled with it. **Still ahead:** per-instance radar, per-seat window hit-testing (a seat's cursor should only reach its own bar), the mirrored top-row layout for rows 2+, per-instance `ControlBarScheme` (currently one shared scheme = one shared skin/faction look), and the ~40 `"ControlBar.wnd:*"` lookups still outside ControlBar.cpp (ControlBarCommand/Observer/Scheme/Beacon/OCLTimer, W3DControlBar, InGameUI, CommandXlat). Compiles and links; eight simultaneous bars are not runtime-verified.

- 2026-07-26 · WP8/WP7 · **Open problem inventory (end of session).** Verified fixed this session and NOT to be re-reported: seat-0 bar docks to its own viewport; per-bar skin; per-bar radar blips; move-hint owner tag; tire tracks; the stencil-shadow and occlusion-silhouette full-screen quads (were covering `TheTacticalView`'s rect only); seat cursor art; per-seat bars destroyed on match exit. Deliberately dropped from scope by the user: per-seat window hit-testing, mirrored top-row layout. **OPEN:** (1) **Input: two parallel wirings is itself the bug (architectural, highest value).** Seat 0 gets full control for free because its keyboard/mouse events flow through CommandMap -> MetaEventTranslator, which turns them into every `MSG_META_*` the game has. A pad seat instead has a HAND-WRITTEN list of emitted messages in `SeatManager::createStreamMessages` - currently cursor, left/right click, `MSG_META_STOP`, `SELECT_NEXT_IDLE_WORKER`, `VIEW_COMMAND_CENTER`, `SELECT_MATCHING_UNITS`, camera scroll/rotate/zoom - so every control has to be re-implemented per seat and will always lag seat 0. Fix: a pad emits seat-tagged RAW KEY events through a pad-button->key binding table, so the existing CommandMap/MetaEvent chain produces the commands. The seat tag already survives derived messages (`TheSeatActingSeatOverride`), so control groups, hotkeys, attack-move, waypoint mode etc. come for free and there is only ever ONE wiring to maintain. (2) **Money display mislocated** - scaled but not translated (renders at viewport origin + (0, displayH*scale)). (3) **Generals-powers purchase buttons mislocated.** (4) **Army/faction decal missing** on bars 1..N. NOTE: (2)(3)(4) all smell like ONE bug in how layouts other than ControlBar.wnd get positioned - fix together. (5) **Radar shroud still shared** - push-based single texture written as cells change; needs one per player (only the blips were made per-bar). (6) **Radar view-box triangle missing on seat 1** - `drawViewBox`/`reconstructViewBox` derive from the `TheTacticalView` global; same bug class as the shadow quads, fix with `rts::getRenderViewRect`/the bar's view. (7) **Decals leak** (spy-satellite reveal circle, radius cursors) - `CLASSID_IMAGE3D` objects, which `renderOneObject` draws with NO shroud test at all (the probe reports this verbatim). Needs the `DrawableInfo::m_seatOwnerPlayerIndex` tag that fixed move hints. (8) **Units still ghost for other players** - ghost owner filter not catching every case; get a probe row on a specific offender. (9) **Civilian/neutral buildings missing from seat 1's fog - REGRESSION from this session's owner filter.** Suspect `Visibility_Check`'s `peekShroudedStatus` is too aggressive for neutral KINDOF_IMMOBILE structures, where `m_everSeenByPlayer` governs fogged visibility (PartitionManager.cpp:1665-1690). Making things vanish that should be visible is worse than a leak - fix before anything else.

- 2026-07-26 · WP7/WP8 · **Nine-problem sweep; all nine addressed, one build.** (1) **Input: the two wirings are now one.** A pad no longer emits a hand-picked list of `MSG_META_*`; its buttons are delivered as seat-tagged `MSG_RAW_KEY_DOWN/UP` through a `seatButtonToKey()` table, so CommandMap -> MetaEventTranslator produces the commands exactly as it does for the keyboard. Control groups, abilities, attack-move and anything added later now work for a pad seat as soon as they work for the keyboard, and are re-bindable in CommandMap.ini. Clicks, the shift modifier and seat join/leave stay direct (KEY_NONE in the table). (2)(3)(4) **Money, purchase buttons and the army decal were ONE bug:** `ControlBarScheme::init` re-positioned and re-sized named bar windows via GLOBAL name lookups at full-display coordinates - so only seat 0's windows were ever touched, and they were placed outside the docked bar. It now takes a `ControlBar*`, resolves through `bar->findBarWindow`, and maps every position through `bar->dockedPoint()` with sizes scaled by the dock. The shared `ControlBarSchemeManager` gained `setApplyToBar()`, set by the bar before each scheme application. (5) **Radar shroud** now refilled per viewport via new `PartitionManager::refreshRadarShroudForRenderPlayer()`, called immediately before the shroud image draws - the same consume-it-straight-away trick as the terrain fog. (6) **Radar view box** resolves the drawing seat's view (`radarViewForRenderPlayer()`) instead of the `TheTacticalView` global. (7) **Decals: OWNER, not shroud** (user correction - an enemy radius cursor should never be visible at all). The base game kept them private by not CREATING them for non-local players, which breaks with several local players: seat 1 never got its own, and whatever was created was drawn by every viewport. `createRadiusDecal` now creates for any local seat's player and records the owner on the `Shadow`; the decal pass skips decals owned by another player. (8) **"Ghost units" were stencil shadows** (user's read, and correct): the volumetric shadow loop gated only on `m_isEnabled`, never on the caster being visible in this viewport - unlike the projected-decal path, which always did. A hidden unit still cast its volume, drawing a dark unit-shaped shape. (9) **Civilian buildings vanishing - my regression, now fixed:** only `OBJECTSHROUD_SHROUDED` means "never seen, do not draw"; `FOGGED` means "seen before, draw darkened", which is how scouted buildings appear greyed in fog. New `isHiddenFromPlayer()` tests `== SHROUDED`, and the `==` matters as much as the constant because `OBJECTSHROUD_INVALID_BUT_PREVIOUS_VALID` sorts ABOVE `SHROUDED`, so every `>=` test also hid objects that were merely mid-recompute. Build+link clean (generalszh.exe); all nine unverified at runtime. Note: `Core/.../RadiusDecal.cpp` now calls `Shadow::setOwnerPlayerIndex`, added to GeneralsMD's `Shadow.h` only, so the already-broken Generals build diverges a little further.

- 2026-07-26 · process · **Scripted bulk edits bit twice in one session; both were the same mistake.** (a) A blanket replace of `ThePlayerList->getLocalPlayer()` -> `getBarPlayer()` also rewrote the call INSIDE `getBarPlayer()`'s own fallback, so every call recursed to a stack overflow. (b) A regex that inserted `setApplyToBar(this)` before each scheme call matched two COMMENTED-OUT lines - the inserted lines were not commented, so dead calls in `setDefaultControlBarConfig` became live, and since the scheme calls that function, it recursed to a stack overflow again. The same regex also hit three BRACELESS `if` bodies, making guarded calls unconditional (a null-deref waiting to happen). **Rules for next time:** after any scripted edit, (1) check whether the anchor appears inside a comment, (2) check whether the statement it attaches to is a braceless `if`/`else`/loop body, and (3) check whether the function being called is one the edited function is reachable FROM. A clean compile proves none of these three.

- 2026-07-27 · WP7/WP8 · **Nine-problem sweep #2; one root cause explained two of them.** (1)(4) **Units leaking through the fog and civilian buildings vanishing were the SAME hole.** Every extra viewport decides what to draw from the CACHED per-player shroudedness (`PartitionData::friend_peekShroudedness`, read by `RTS3DScene::Visibility_Check` and by the ghost system's `anyOtherLocalSeatSees`), and that cache is only filled by an actual `getShroudedStatus(player)` call. `GameClient::update` only asked about the OTHER players for objects that own a ghost object - i.e. buildings - so every UNIT stayed `OBJECTSHROUD_INVALID` for the other seats' players. INVALID is not SHROUDED, so it read as "visible" and each player watched the other's units move through unexplored ground. The same gap made `anyOtherLocalSeatSees` answer "nobody else can see it" when it simply had not been asked, so one seat fogging a building displaced the real render object out of the shared scene and left the other seat a hole where a supply dock or civilian building should be. Fix: while the screen is split, evaluate every drawable's object for every bound seat's player each frame. Cost is one extra shroud evaluation per object per extra seat; the ghost-object special case is kept for the non-splitscreen path. (1b) The scene holds only ONE snapshot per object - whichever seat fogged it last - and the owner filter hid it from everyone else; it now shows the stand-in to any viewer that has its own snapshot of that object (`hasSnapshotForPlayer`, which existed and was unused), since they all depict the same building in the same place. That closes the "known corner case" logged on 2026-07-26. (2) **The placement preview is not a world object** - no object, no shroud status - so the filter passed it and player 2 watched player 1 pick a spot. Tagged with `DrawableInfo::m_seatOwnerPlayerIndex` via a shared `InGameUI::seatOwnerPlayerIndex()` (promoted out of W3DInGameUI.cpp, where the move hints already used it), for both the single icon and the line-build tiles. Its BIB - the translucent square under it - leaked separately: bibs go into one batched vertex buffer with no per-view notion at all. Bibs keyed to an OBJECT stay shared (real building aprons); bibs keyed to a DRAWABLE now read the owner off that drawable, and the buffer is rebuilt per viewport while split. (3) **Superweapon decals:** `createRadiusDecal` only records an owner for templates flagged `OnlyVisibleToOwningPlayer`, and the superweapon cursors are not flagged - the base game kept them private by only ever creating them for the local player, so "exists" meant "mine". `InGameUI::setRadiusCursor` now stamps the owner regardless of the flag (new `RadiusDecal::setOwnerPlayerIndex`), and the shortcut path resolves its object through `getCommandActingPlayer()` instead of the local player. (5)(7) **Money, purchase buttons and the missing skin were the dock fighting the scheme.** `dockToRect` re-applied the .wnd-authored geometry from scratch every frame, which silently threw away everything `ControlBarScheme::init` and `setDefaultControlBarConfig` had positioned - both write DOCKED coordinates through `dockedPoint()`. The money readout ended up at the viewport's left edge because that is where ControlBar.wnd authors it. The dock now remembers what it last wrote per window; if a window no longer holds that, somebody else placed it, so the authored value is recovered by undoing the transform that was in force and the new one applied on top. Read back after writing so a gadget adjusting its own size cannot be mistaken for a re-position. Also: the scheme resolved `GeneralsExpPoints.wnd:GenExpParent` GLOBALLY though each bar creates its own, so every seat's skin wrote over seat 0's window; and the shared scheme manager's draw scale is now set by the bar that is DRAWING rather than by whichever docked last. (6) **Radar view box** was clipped to `m_radarWindow` - resolved once by a global lookup, so always seat 0's - which discarded every line of seat 1's box; the radar is now told which LeftHUD window it is painting (`Radar::setDrawWindow`), and `m_viewBox` is rebuilt per viewport instead of once per camera move. (8) **Trees and props** cull ONCE per camera *move* into flags stored on the tree, and the vertex buffer is built from those flags - so the first viewport to draw decided what every other one could see, and seat 1's trees vanished whenever seat 0 looked away. Both buffers now re-cull whenever the camera differs from the one the flags were computed against. (9) **The crash** (`drawSpecialPowerShortcutMultiplierText` -> `GadgetButtonGetData(null)`): `m_currentlyUsedSpecialPowersButtons` comes from the player TEMPLATE - how many shortcuts the general has, not how many windows were found - and every loop in the file walks the array to that count and dereferences it unchecked. With a bar per seat a scoped lookup can legitimately come back empty. The count is now clamped to the contiguous range that actually resolved, the remaining loops are guarded, and `populateSpecialPowerShortcut` uses the same "is this our army" rule as `initSpecialPowershortcutBar` instead of `isLocalPlayer()` (which left seats 1..N with a built but never-filled superweapon strip). Build+link clean (VS18 BuildTools x86 Debug, generalszh.exe). **All nine unverified at runtime.** Still open and deliberately not attempted: the radius cursor is still ONE cursor shared by all seats (owner-tagged, so it no longer leaks, but seat 1 cannot aim its own superweapon while seat 0 is aiming); `handleBuildPlacements` runs only for seat 0's context, so only seat 0's preview follows a cursor; the scheme is still one shared skin for all bars.

- 2026-07-27 · WP1/WP8 · **Round 3. The input bug was an edge-lifetime bug, and the bar geometry fix from round 2 was itself wrong.**

  **(A) A pad seat could move its camera but never click - and it was never a routing problem.** The decisive readout was `seat 1 ... msgs=1` after seven minutes of play: exactly ONE button message had ever been emitted for that seat, and it was the A-press that JOINED it. Meanwhile the stick worked. That split is the whole diagnosis: `SDL3InputManager::readGamepadState` computes `buttonPressed`/`buttonReleased` against `entry.prevLogical` and immediately advances `prevLogical`, so an edge is true for exactly ONE poll and the next poll produces a state with every edge false. `setSeatInput` then *overwrote* the seat's stored state with it. Polling and consuming are not on the same clock - the SDL pump runs whenever the engine services the OS, `createStreamMessages` runs once per client update - so presses were routinely destroyed before anything looked at them. LEVELS (sticks, triggers, held buttons) are re-read every poll and so survived, which is exactly why the camera worked and the buttons did not. Fix: `setSeatInput` latches edges (OR-in) and only `consumeSeatInputEdges` clears them, called at the end of each seat's emission. The join press is consumed by the join itself, which also retires the long-standing "A-to-join also fires a click" artifact. Seat 0's edges are dropped at route time because nothing consumes them (the backend's legacy injection handles its buttons directly).

  **(B) Money read $0 for player 1 while player 2's readout showed player 1's cash.** `InGameUI::update` found "the" money window with a GLOBAL name lookup and filled it from `TheControlBar`. With a bar per viewport there are several identically named MoneyDisplay windows: the lookup returned an arbitrary one, that one got another player's money, and every other bar was never updated at all. The `lastMoney`/`lastIncome` function statics made it worse by suppressing updates across bars. Moved to `ControlBar::updateMoneyAndPowerDisplay()` - own window via `findBarWindowById`, own player, own change-cache - driven for every instance from `ControlBarInstances::updateMoneyAndPowerAll()`.

  **(C) Seat 0's bar parked exactly one bar-height above its viewport floor (`dock=(0,732 ...)` where seat 1 correctly read `906`). My round-2 change caused it.** That change made `dockToRect` INFER authored geometry: if a window no longer held what the dock last wrote, it assumed a deliberate re-position and recovered a new authored value by undoing the transform. That is unsound - several places (`ControlBarResizer` among them) write full-display coordinates directly - and one such write got baked in permanently, at which point the bar computed its own wrong position forever after. Replaced inference with an explicit contract: `ControlBar::placeBarWindow(win, authoredX, authoredY, [w,h])` and `resizeBarWindow` are now the only sanctioned way for anything outside the dock to move a bar window; they record the AUTHORED value and apply the current transform, so the next dock reproduces the placement instead of fighting it. `ControlBarScheme::init` (all nine placements) and `setDefaultControlBarConfig`/`setSquishedControlBarConfig`/`setLowControlBarConfig` now go through it. This is also what should put seat 1's generals/hide buttons in its own corner - they were being written at full-display coordinates and full size.

  **(D) Player 1's cursor reverted to the plain arrow whenever player 2 moved.** `LookAtTranslator` is one shared object and `stopScrolling()` writes `TheMouse->setCursor(prevCursor)` - the shared OS cursor, from a FILE-STATIC `prevCursor` - as well as `TheInGameUI->setScrolling` and the tactical view's mouse lock. A pad seat's `MSG_RAW_MOUSE_POSITION` reaches this translator and its cursor sits at its own viewport edge constantly, so it kept flipping seat 0's scroll state. Added `getCommandActingSeat()` (the seat whose message is being translated; 0 for keyboard/mouse, replays and network) and made `setScrolling`/`stopScrolling` no-ops for any other seat - a pad seat scrolls its own view with its own stick and has its own software cursor, so none of that shared state is its to write.

  **(E) Instance 0's global-lookup fallback could hand it seat 1's widgets.** `findBarWindowById` keeps a global fallback for instance 0, but the global lookup walks every window in the manager and per-seat bars carry identical names - so seat 0 could style and position ANOTHER viewport's bar while leaving its own untouched. The fallback now refuses anything another instance owns.

  **(F) Two problems are still not explained, so this pass instruments rather than guesses** (the lesson from the three sessions logged above): "player 2 sees player 1's units" and "civilian buildings/supply depots only appear for player 2 while player 1 can see them". Ruled OUT by reading: the visibility pass is NOT shared between viewports (`Visibility_Checked` is cleared immediately after each `Customized_Render`, so `Visibility_Check` genuinely runs per view); `OBJECTSHROUD_INVALID` is 0, so it sorts BELOW `FOGGED` and the union test in GameClient treats it as "can see", which is the safe direction; and on paper the union already clears `m_drawableFullyObscuredByShroud` when any local seat can see the object. So the remaining cause is something the code cannot tell us. The probe row now prints EVERY local seat's cached shroud status (`s<seat>/P<player>:<status>`) plus the drawable's global `obsc=` flag, which distinguishes the three candidates in one screenshot: a stale `0` (INVALID) means nothing evaluated that player this frame; `obsc=1` means the drawable is hidden in every viewport at once; and a correct-looking row means the leak is downstream of the filter. Point the mouse at the offending thing and read the row.

  Build+link clean (VS18 BuildTools x86 Debug, generalszh.exe). (A)-(E) unverified at runtime.

- 2026-07-27 · WP8/WP7 · **Round 4. The per-seat bar was never given a skin, and the test run had no real pad on seat 1.**

  **(A) Money in the corner, no buttons, no army decal for player 2 - all one omission: nothing ever applied a ControlBarScheme to a per-seat bar.** The classic bar gets one when the game tells it which side the local player is; `initAsSeatInstance`/`syncToSeats` never told a seat bar anything, so it kept ControlBar.wnd's raw authored layout. The arithmetic confirms it exactly: seat 1's money readout sat at (960,545), which is its dock offset (960,540) plus an authored y of ~10 scaled by 0.5 - i.e. precisely where the .wnd authors MoneyDisplay, untouched. The scheme is what moves money, worker/options/beacon/generals buttons and the faction artwork into place, so all four symptoms are the same missing call. New `ControlBar::applySchemeForBarPlayer()`, idempotent per player template (the scheme re-positions two dozen windows, so it must not run per frame), called from `syncToSeats` after the dock.

  **(B) Text did not shrink with the bar.** `winSetSize` scales a window's box and does nothing to its text, so a docked bar drew full-size glyphs in half-size widgets - the money plate overflowed and the generals screen's labels ran over their rows. `AuthoredWindowGeom` now records the authored font (name/points/bold) and the dock asks `TheFontLibrary` for the same face at `points * scale`.

  **(C) `anyOtherLocalSeatSees` was skipping seat 0, and the overlay proves it: `seat 0 ... ply=-1`.** Seat 0 never gets an explicit player index - it IS the local player - but that function read `m_playerIndex` straight off the seat and `continue`d on anything negative. So for every object a NON-seat-0 player was last to fog, it answered "no other local seat can see this", that player's grey snapshot displaced the real object out of the shared scene, and seat 0 was left with a hole where a building it can plainly see used to be. This is the "known corner case" logged on 2026-07-26, now with a concrete mechanism. Fixed at the point of use (falls back to the ghost manager's local player for seat 0); the probe's per-seat column does the same so seat 0 stops being omitted from the readout.

  **(D) The run that produced this round's screenshot could not test the input fix at all.** The seat table reads `seat 1 st=3 dev=-101 ply=3` and seats 2..7 `dev=-102..-107`: those are the `-splitscreendev <n>` FAKE seats (`SEAT_DEVICE_FAKE_BASE = -100`, seat i gets `-100 - i`), and `seat 1 ... msgs=0` confirms no device input reached it. So "player 2" in that run was a harness seat with no controller behind it, which can never click regardless of the edge-latch fix. `bindSeatToDevice` does evict a fake seat for a real pad, but only the HIGHEST one - a real pad joining here lands on seat 7, not on the seat that holds the player. Worth revisiting; for now the input fix needs a run with plain `-splitscreendev` (no count) so the pad takes seat 1 directly.

  **(E) The probe row the user captured, read out:** `46px stencil AmericaVehicleDozer own=P3 ss=2 ghost=P-1 -> DREW darkened by shroud material pass s1/P3:1 obsc=0`. `ss=2` is `OBJECTSHROUD_PARTIAL_CLEAR` for the viewport's player, and `isHiddenFromPlayer` only hides `== SHROUDED`, so this draw is the rule working as written, not a filter miss: some of the cells that dozer stands on are genuinely clear to that player. Note the enemy/neutral special-casing in `PartitionData::getShroudedStatus` only runs in the FOGGED branch - PARTIAL_CLEAR has no such guard, in vanilla either - so an enemy unit straddling the edge of your vision is drawn. Whether that is the reported leak depends on whether the vision is real; it needs a probe on a unit that is unambiguously deep in fog. Civilian buildings still need a probe row of their own, taken in the viewport where they are MISSING.

  Build+link clean (VS18 BuildTools x86 Debug, generalszh.exe). All unverified at runtime.

- 2026-07-30 · WP7/WP8 · **The 12-bug live-play sweep. Four fixed in the previous session, the remaining eight here; two of the eight turned out to be one root cause, and so did two more.**

  The first four (committed separately as `56872d8fd`): the tenth-parameter compile break that stopped the branch building at all; the match-restart crash, which was `m_barAuthoredGeom` docking through windows `initSpecialPowershortcutBar` had already freed (the AudioManager frames in the stack were whatever got allocated over the corpse); "easy army" everywhere, which was `GameSlot::setState` discarding a supplied name for any state but `SLOT_PLAYER`; and a Random CPU still reading as concealed on the load screen.

  **(A) Nothing clips a per-seat control bar to its seat's viewport, and that is two of the bugs.** A bar is authored against the whole display and then scaled into one viewport, so parts of it legitimately fall outside that viewport - most obviously the "hide the bar" stage, which places the root at 90% of the AUTHORED display height, i.e. with most of its height below the viewport floor and on top of the player sitting underneath. Player 1 pressing hide put his bar on player 5's screen. The window system clips nothing, anywhere. New `GameWindowManager::drawTopLevelWindow` asks `ControlBarInstances::clipRegionForRootWindow` whether the window it is about to paint belongs to a bar docked into a sub-rectangle, and paints the subtree through `drawWindowClipped` if so. The clip is re-asserted around *each* window's draw rather than set once, because several gadget draw functions clip a piece of their own artwork and then just switch clipping off when they are finished with it. Undocked bars (the whole shell) and full-display docks return no rect, so single-viewport play is untouched.

  **(B) Text ignored every clip rectangle in the engine, which is why it "overflowed instead of being cut off".** `W3DDisplay::drawImage` honours the display's clip; text does not go through it at all - it is built and rendered by the display string's own sentence renderer, which clips against a rectangle of its own that nothing was setting. New `Display::getClipRegion` (non-pure, base answers FALSE) lets `W3DDisplayString::draw` adopt whatever the display is currently clipping to. It has to feed `needNewPolys`: the sentence renderer clips per glyph *as it builds the quads*, so a changed rectangle does nothing at all until they are rebuilt. A string that sets its own region explicitly (list boxes, the mouse tooltip, IME candidates) keeps it - that one is tighter and more specific.

  **(C) The world-space overlay pass was drawn with the wrong camera and confined to nothing - which is "player 1 builds something, moves the camera, and it shows in other viewports".** Health bars, veterancy chevrons, unit captions and the "under construction" percentage are 2D artwork positioned by projecting a world point onto the screen, and every one of those projections goes through the `TheTacticalView` GLOBAL (`Drawable::drawHealthBar`/`drawCaption`/`drawConstructPercent` all call `TheTacticalView->worldToScreen`). `Display::drawViews` sets a render-player override and a render-view rect per view but does NOT repoint that global, so each extra viewport placed its own player's overlays using seat 0's camera - and since the result is not clipped either, an object at the edge of a viewport projects to coordinates in the NEXT player's viewport and draws there. `W3DView::draw` now points `TheTacticalView` at the view being drawn for the duration of the post-draw pass and clips the pass to that view's rectangle. This is the same choke-point swap `MessageStream::propagateMessages` already uses to make a seat's orders resolve in its own view, and for the same reason: threading a `View*` through every projection site is a much larger and much more error-prone change. Both halves are gated on a second view existing, so a single-viewport game runs exactly the code it always ran.

  **(D) The generals screen opened empty on the LAST player's viewport, and the mechanism is name collision.** `TransitionWindow::init` resolves each window it animates with `winGetWindowFromId(nullptr, id)` - a walk of every window the manager owns. That is unambiguous while `GeneralsExpPoints.wnd` exists once; with a bar per seat there are eight identically named copies and the walk returns whichever it reaches first, in practice the most recently created bar. So the `GenExpFade` transition played on seat 7's science window - a window nothing had populated, because `populatePurchaseScience` had run on the bar whose button was actually pressed. `GameWindowTransitionsHandler::setWindowLookupScope` lets a caller resolve a group inside specific window trees, `ControlBar::setTransitionGroupForBar` wraps `setGroup` in this bar's own roots, and a name that resolves nowhere inside them still falls back to the global lookup so every shell menu is unaffected. The generals button callback also went through `TheControlBar` unconditionally; it now routes through `ControlBarInstances::fromWindow`, like the other bar callbacks already do.

  **(E) No seat but the first ever had a superweapon strip, because the game only ever builds one.** `initSpecialPowershortcutBar` has exactly ONE caller in the entire codebase - `GameLogic.cpp:2318`, at match start, with the LOCAL player. Per-seat bars are created much later, from `InGameUI::updateSeatViewports`, once a seat has both a viewport and a player; by then that call is long gone and nothing else ever built one. New `ensureSpecialPowerShortcutBarForBarPlayer`, driven from `syncToSeats` next to `applySchemeForBarPlayer` and idempotent per player template for the same reason (it creates a whole .wnd layout, so it must not run per frame).

  **(F) The slide-in swept across every viewport because it starts one whole DISPLAY away.** `ProcessAnimateWindowSlideFromRight::initAnimateWindow` puts the window at `restPos.x + TheDisplay->getWidth()`, so a bar resting inside a quarter-width viewport began its travel off the right of the whole window and crossed everyone else's screen to get home. `ProcessAnimateWindow` gained animation bounds (zero = the whole display = classic behaviour), `AnimateWindowManager::setAnimationBounds` pushes them to every process it owns - each manager owns its own set, which is what makes this per-bar - and `dockToRect` sets them from the rectangle the bar is docked to. The speed is scaled by the same ratio, or a quarter-width bar would snap home four times as fast as the classic one.

  **(G) The load-screen number badge was concealment, not a splitscreen bug at all.** `updateMapStartSpots` only writes a badge for a slot whose *apparent* start position is real, and `getApparentStartPos` returns `m_origStartPos` (-1, "random") for everyone the local slot is not allied with. Only slot 0 is allied with itself, so only the keyboard player got a badge. Same shape as the Random-army bug fixed last session and now the same fix: `isSlotInLocalSkirmish` also short-circuits `getApparentStartPos`, `getApparentColor` and `getApparentPlayerTemplate`. Concealment exists to stop an opponent counter-picking; a local skirmish has no opponent to hide from, every slot is sat at the same screen, and by load-screen time `populateRandomStartPosition` has already rolled the real value (`GameLogic.cpp:1318`, immediately before the load screen is created at :1325).

  **(H) Seat cursors were the right art at the wrong scale because the two cursor paths measure different things.** Player 1's cursor in the hardware-cursor mode is the raw `<textureName>.tga` surface handed to `SetCursorProperties`, which D3D draws at that surface's pixel size. A seat cursor goes through `TheDisplay->drawImage`, whose 2D coordinate range is pixel-for-pixel with the display - so the two agree exactly *when the measurement is the same*. It was not: the renderer prefers the MAPPED image (so that a cursor defined as an atlas entry does not drag its whole atlas page onto the battlefield) and a mapped entry carries its own declared size, which need not match the texture's. It now takes the ART from the image and the SIZE from the cursor texture whenever that can be measured, so the two cannot diverge by construction, and the probe reports the size actually DRAWN so one screenshot confirms it. No multiplier was guessed, as the handoff asked.

  Build+link clean (VS18 BuildTools x86 Debug, `z_generals` → generalszh.exe, 18.7 MB). **All eight unverified at runtime.** Note for the next run: the handoff asked for (D) and (E) to be retested on the current build before being worked on, in case they were symptoms of the restart crash - that retest did not happen, so if either was a crash artifact these are additive fixes to a problem that had already gone. Both changes are correct independently of that. Also still open and deliberately untouched: `theAnimateWindowManager` in ControlBarPopupDescription is a FILE STATIC shared by every bar, so the build tooltip is one popup for all seats and its slide-in is not bounded to a viewport; and `winFindChildById` still exists only in GeneralsMD's `GameWindowManager`, which Core's `GameWindowTransitions.cpp` now depends on - one more thing the Generals port pass will have to carry over.

- 2026-07-30 · WP1/WP7/WP8 · **Round 2 of the live-play sweep (9 reports). The pad input answer is the important one: the TRANSLATORS are the shared state, not the routing.**

  **(A) Why a pad seat could move its camera and its cursor and do nothing else.** Both of those are done by DIRECT calls inside `SeatManager::createStreamMessages` - `s.m_view->scrollBy()` and `s.m_cursor.pos`. Everything else goes through the message stream and into the translator chain, and **the translators are single-instance state machines**. Three of them were provably corrupting each other across seats: (1) `MetaEventTranslator::m_mouseDownPosition` / `m_nextUpShouldCreateDoubleClick` are indexed by BUTTON only, and they are what turns a raw down/up pair into a cooked click - so a pad's click, taken while the mouse player had pressed somewhere else, produced a pixel region spanning both cursors, which reads as a DRAG, so the click selected nothing. (2) `CommandTranslator::m_mouseRightDragAnchor/Lift/Down/Up` likewise, and EVERY right-click order is gated on `Mouse::isClick()` over them. (3) the seat stamped its click times by copying `TheMouse->getMouseStatus()->time`, i.e. whenever player 1 last moved the physical mouse - so the moment player 1 touched the mouse between a pad's press and release, `isClick` measured a multi-second gap and discarded the order as a drag. All three are now per seat, and the seat has its own millisecond clock. Intermittent-looking symptoms with a completely mechanical cause.

  **(B) One input wiring, actually.** `seatButtonToKey` was a second, thinner table than the legacy pad mapping in `injectLegacyMouseKeyboard` - no control groups (the d-pad had been taken for camera rotate/zoom), no attack-move, no shift-to-queue. It is now key-for-key identical to the legacy mapping, so a pad on any seat has exactly what a pad on seat 0 has, and re-binding in CommandMap.ini covers both. The one deliberate difference: the right stick still scrolls the seat's own view directly instead of injecting arrow keys, which is strictly better and is the part that already worked. `TheKeyboard->isShift()` in SelectionXlat now asks the acting SEAT (`getCommandActingShift()`) - a pad has no keyboard, so that call was answering for whoever sits at the keyboard.

  **STILL OPEN for pad input, and it is the honest remainder:** `WindowTranslator` returns early for any seat but 0, so a seat's clicks cannot reach its own control bar. Per-seat window hit-testing was dropped from scope earlier; it is now the largest thing standing between a pad seat and full parity, because the control bar is where building and abilities live.

  **(C) Hiding player 1's bar toggled player 5's too, and the animation only ever played on one bar - one cause.** `ShowControlBar`/`HideControlBar`/`ToggleControlBar` each resolved "the" `ControlBarParent` with a GLOBAL name walk and then drove `TheControlBar`'s animator. With eight identically named copies the walk returns an arbitrary one. All three now resolve through the bar belonging to `getCommandActingSeat()`, which is `TheControlBar` for the keyboard/mouse and for every single-bar game.

  **(D) Player 1's viewport blowing back up to the whole window was the same keypress.** `setFullViewportHeight`/`setScaledViewportHeight` resize the `TheTacticalView` global to a fraction of the whole DISPLAY, and every control-bar stage change calls one of them. So hiding the bar restored seat 0's view to full size on top of everyone else. Both are now no-ops while more than one seat is bound - the viewport layout owns view geometry then, the same call V6 made for the letterbox.

  **(E) The generals-action decal showed for everyone because the owner filter was on the wrong list.** `W3DProjectedShadowManager::renderShadows` walks TWO lists. The filter added on 2026-07-27 went on `m_shadowList`; `addDecal()` - which is what `createRadiusDecal` calls - fills `m_decalList`, whose loop had no owner test at all. Radius cursors, reveal circles and targeting rings therefore drew in every viewport regardless of the owner tag they were carrying. Same filter, now on the list that actually holds them.

  **(F) The radius cursor was ONE object for the whole game.** `m_curRadiusCursor`/`m_curRcType` were plain members, never moved into the per-seat UI context. So arming a power on any seat first CLEARED whoever else had one - player 8 aiming a superweapon deleted player 1's ring - and `handleRadiusCursor` then aimed it with `TheMouse` through `TheTacticalView`, i.e. player 1's pointer and player 1's camera. Now one ring per seat, each aimed with its own cursor through its own view, each stamped with its own player so (E) can tell them apart.

  **(G) "Easy army" in the lobby, and why the name reverted.** `UpdateSlotList` displays anything that is not already `isHuman()` by doing `GadgetComboBoxSetSelectedPos(combo, slot->getState())` - which re-selects the "Easy AI" dropdown entry EVERY frame. A seat claims its slot as an AI on purpose (that is what makes the match spawn it an army; SeatManager converts it to human at start), so the claim wrote the player's name and the very next repaint wrote over it. A slot claimed by a local seat is now presented like a human's: its name, no difficulty dropdown.

  **(H) `-splitscreendevquiet`** does everything `-splitscreendev` does but leaves the seat debug overlay off. The overlay is drawn at the top-left of the screen, which in a split game is player 1's own viewport, so it cannot be read around while judging how the game looks.

  Build+link clean (VS18 BuildTools x86 Debug, generalszh.exe). All unverified at runtime. Process note: the scripted rename in CommandXlat mangled the constructor and half the call sites exactly as the 2026-07-26 entry warns - caught by reading the diff, not by the compiler, since the wrong-but-valid form still compiled in two places.

- 2026-07-31 · WP8 · **A seat can press its own control bar (handoff2 §5.1 closed).** `WindowTranslator::translateGameMessage` returned early for every `getSeatIndex() != 0`, so no seat but the keyboard/mouse touched the window system at all - the reason a pad seat could select and command units but could not press a single button on its own bar. The ban existed because everything the window system keeps is singular: one hover window, one grab window, one mouse captor, one tooltip. A left-down records the grab window so the matching left-up reaches it even after the cursor leaves, so two seats clicking at once measured each other - the same shape as the per-button click/drag state that had to go per seat in MetaEventTranslator and CommandTranslator (bug class 3 in handoff2 §5.2). Fix, in three parts: **(A)** `GameWindowManager::winBeginSeatInput/winEndSeatInput` swap the hover/grab/captor trio for the seat's own copy around one message, via an RAII scope in WindowXlat so the several early returns cannot skip the restore. Seat 0 deliberately keeps using the shared fields rather than a slot of its own - they are what the rest of the engine reads - so a single-seat game runs the untouched code path. **(B)** `findWindowUnderMouse` now skips windows the acting seat may not touch (`winSeatOwnsWindow`, resolved through `ControlBarInstances`/`ownsLayoutWindow`). Eight instances of ControlBar.wnd are eight sets of identically named windows and that walk knows only about position; geometry nearly answers it since the viewports are disjoint, but the bar is authored against the whole display and then scaled, so parts of it legitimately sit outside its dock rect. Windows owned by no bar (quit menu, diplomacy, message boxes, shell) stay with seat 0. **(C)** The tooltip is only written by the seat holding the OS mouse - its callbacks drive the shared popup description as a side effect - and the mouse-locked check asks `getCommandActingView()` instead of `TheTacticalView`, which had been suppressing a seat's input because *player 1* was scrolling. Keyboard events stay seat-0-only on purpose: a pad's buttons are synthesized as key messages for the meta/command translators, and the window system would type them into whatever holds the single global keyboard focus. Both window-destroy paths scrub the per-seat copies too (`winForgetSeatWindow`) - a seat's bar dies whenever its seat goes away or the match ends, and a stale pointer there is the same freed-window crash that once read as an audio bug. Commit `83ca81b0a`; build clean; **unverified at runtime**.

- 2026-07-31 · harness · **Fake dev seats now WATCH live AI players instead of taking them over (user request).** The harness converted each fake seat's army from AI to human (`setPlayerType(PLAYER_HUMAN)`), which stops its brain - so every extra viewport showed a base standing still. Nothing worth looking at means nothing worth testing: health bars, "Building %d%%" captions, the superweapon strip and bar clipping all need an army that moves, which is part of why those checks kept coming back inconclusive. An **observer seat** (`LocalSeat::m_observer`) binds `m_playerIndex` to an existing AI player and simply does not convert it. Everything downstream already keys off `m_playerIndex` and does not care whether the player is human - the view's render player (per-player fog), the per-seat ControlBar, the debug overlay - so binding was the whole of the wiring. Around that: the lobby claim is skipped for observer seats (an AI is dropped into an *empty* slot only, so `-splitscreendev 3` still produces 3 viewports on a map whose default lobby fills only slot 1; occupied slots are never touched, renamed or re-sided); observer seats emit no cursor and no stream messages; and `updateSeatFollowCamera` eases each observer camera toward its player's base every frame. **Camera rule:** the camera **locks onto ONE unit and follows it** until the next swap - it does not re-aim every frame. That distinction was a live bug, caught by the user on the first pass: re-electing "the unit nearest the anchor" each frame meant a *different* unit won as things moved, so the camera slid between them and read as drifting rather than following. The unit is picked only when the shot changes (`m_followObjectID`, stored as a raw ObjectID value to keep the Core header off GameLogic), or immediately if the followed unit dies - waiting for the timer there would park the camera on the spot where it died for up to fifteen seconds. Two shots alternate on a randomised 10-15s timer (`SEAT_FOLLOW_HOME` / `SEAT_FOLLOW_FRONTLINE`), because either alone misses half of what an army does. **Home** = the unit nearest the centroid of the player's own structures - where building and production happen. **Frontline** = the unit nearest ANY *enemy* base centroid, i.e. whichever has pushed deepest into somebody else's territory - where the fighting is. Position is eased at 0.05/frame, which also makes the timed swap read as a pan rather than a cut. The interval is randomised per seat so eight viewports do not all cut on the same frame, and comes from `GameClientRandomValue` - never the logic RNG, since all splitscreen state is client-side and must not perturb the sim. Anchor computation now runs only on re-pick rather than per frame, which also disposes of the double-`iterateObjects`-per-frame cost the first version had. Fallbacks: a player with no structures uses the centroid of everything it has; with no enemy bases standing, frontline degrades to the home shot; an army of nothing but structures watches its base; with nothing on the map at all the camera holds position instead of snapping to the map origin. **The takeover path is untouched and is what a real pad still gets** - `bindSeatToDevice` calls `reset()`, which clears `m_observer`, so a real controller evicting a fake seat gets a playable seat, not an observer. New flag `-splitscreendevtakeover [n]` forces the old behavior for hot-swap work; observing is now the default for `-splitscreendev`/`-splitscreendevquiet`. Build clean; **unverified at runtime**.

- 2026-07-31 · round 3 · **Five user-reported bugs, and the one of them the observer change caused.**

  **(A) Every seat's radar was wired to player 1's radar building.** `W3DRadar::draw` opens with `if( !rts::localPlayerHasRadar() ) return;`, and that helper read `ThePlayerList->getLocalPlayer()` unconditionally - so all eight radars went dark the instant player 1 lost its radar and lit up again when player 1 rebuilt one. Bug class 2 (a singleton read during per-view work, silently answering for seat 0) with an exact fix already available: `localPlayerHasRadar` now consults `TheRenderPlayerIndexOverride`, which `W3DLeftHUDDraw` already sets around each bar's radar. `W3DLeftHUDDraw` in turn stops force-drawing for other seats (`isOtherSeat ||`) - that had been papering over the same helper from the other direction, drawing a radar for a player who had none. `LeftHUDInput` asks inside the same override, so a click is eaten or accepted according to whether the radar it landed on has power.

  **(B) The pad on the highest seat drove player 1 first, from a black cell.** Not a routing bug: `bindSeatToDevice` evicts the highest fake seat when a real controller arrives, and eviction called `reset()`, which threw away that seat's `m_playerIndex` and `m_view` along with its observer flag. A seat with no player is excluded from `updateSeatViewports`' seat list (so its cell in the grid goes black) and produces messages with no player override, which every translator therefore attributes to the *local* player - so the pad commanded player 1's army until the next bind caught up. Eviction is now `takeOverSeat()`: the player, the view and the lobby slot stay, the observer flag and camera-follow state go, and the army is taken off the AI right there rather than a frame later. Safety net for every other way a seat can be player-less mid-match: `createStreamMessages` now emits nothing at all for a seat with `m_playerIndex < 0` while a real match is running - silence is right, and falling through to player 1 never is. Menus are exempt: there is no player to act as and the seat still has to press buttons.

  **(C) Changing resolution in the shell left player 1's bar spread across the whole window.** `InGameUI::recreateControlBar` deletes `TheControlBar` and builds a new one, but never handed the new bar the layout roots `createControlBar()` had just made - only `InGameUI::init` did that. With no roots there is no authored geometry, so `dockToRect` had nothing to transform and seat 0's bar stayed at full-display coordinates. One line, the same one `init` uses, before `init()`.

  **(D) Audio for seats 1-7.** Two independent single-listener assumptions. `AudioManager::shouldPlayLocally` asked only about `getLocalPlayer()`, and unit acknowledgements, "construction complete" and EVA are all `ST_PLAYER` sounds - so every seat but the first played in silence. The player-restriction half is now `shouldPlayForListener()`, asked once per person at the machine (`SeatManager::getExtraLocalListeners`); a sound belongs to this machine if it belongs to any of them. `SoundManager::canPlayNow` culled positional audio by distance from the single listener and by seat 0's shroud, which silenced anything happening at another seat's base - now the nearest local listener decides the distance and any local player seeing it clears the shroud test. **Known limitation, not fixed:** the Miles 3D listener is still seat 0's camera, so a positional effect at another seat's base is no longer *culled* but is still *attenuated* from seat 0's viewpoint. Genuinely fixing that needs a per-seat submix, which one listener cannot give.

  **(E) The load-screen reveal was unhiding the enemy CPUs.** `isSlotInLocalSkirmish` answered TRUE for every slot in the skirmish, so last round's fix for "only player 1 gets a start-position badge" removed Random concealment outright rather than narrowing it - enemy armies, colours and start positions were all revealed. Renamed `isSlotLocalSeat` and narrowed to slot 0 plus slots a local seat actually claimed. Allies were never this function's job: every call site already falls through to `!isSlotLocalAlly()`, which reveals them as it always did. An observer seat is deliberately *not* a local seat - nobody sits behind one and its army is an opponent's.

  Build+link clean (VS18 BuildTools x86 Debug, generalszh.exe). All five **unverified at runtime**. Also answered, no code: nothing on this branch touches `m_maxCameraHeight` or `m_enforceMaxCameraHeight` - they are still GameData.ini's (default 300). The only camera limits the branch adds are `SEAT_CAMERA_ZOOM_MIN/MAX` (0.35-2.00) clamping the d-pad zoom *factor* of a pad seat's own view.

- 2026-07-31 · round 3b · **The pad seat went dark instead of wrong, and 3D audio for every seat.**

  **(F) A controller joining mid-match was handed a seat with no army.** The round-3 hand-off only fired when *every* seat was taken, because `findFreeSeat()` was still asked first - and `-splitscreendev 6` deliberately leaves seat 7 free (see the comment on `parseSplitscreenDev`). A free seat is worth nothing once a match is running: seats get their army from a lobby slot claimed *before* the match, so a seat nobody claimed a slot for has no player, no viewport, and - after round 3's silence rule - nothing it can do. That is why the symptom changed from "the pad drives player 1" to "the pad does nothing and the 8th cell is dark": the silence rule was working, and it exposed that the seat was empty. `bindSeatToDevice` now prefers, while a match is running, the highest fake seat that actually HAS a player, and falls back to a free seat only if none does. In the shell and the lobby the old order stands - a free seat is the right answer there, because the lobby claim will give it a slot.

  **(G) 3D audio now works from every seat's viewpoint, not just seat 0's.** Round 3 stopped other seats' sounds being *culled*; they were still *attenuated* as if heard from seat 0's camera, which for a base on the other side of the map is silence by another route. The device has exactly one 3D listener and cannot grow another, so instead each positional sound is expressed in that listener's frame: `AudioManager::remapToListenerFrame` finds the local seat whose camera is nearest the sound, takes the sound's offset from that camera, rotates it by the difference in heading between that camera and seat 0's, and re-anchors it on seat 0's camera. Distance is preserved exactly, so the sound attenuates the way the player watching it should hear it, and left-on-their-screen still pans left. **When seat 0 is the nearest camera the result is bit-identical to the input**, which is what keeps a single-view game untouched. Applied at all three places that measure against the listener: sample placement and the per-frame position update (both funnel through `getCurrentPositionFromEvent`), and `getEffectiveVolume`, which is what the update loop culls a playing 3D sound on - leaving that on raw world coordinates would have gone on silencing correctly-placed sounds. `isOnScreen` was also seat-0-only; it drives the off-screen muffling low-pass, so everything at another seat's base was heard through a wall by the player looking straight at it. It now asks every seat's view. Residual artifact, accepted: a sound exactly equidistant from two seats can flip which camera it is referred to, and since distance is equal at that point the volume is continuous but the panning can jump. Bases are far enough apart that this is rare.

  Build+link clean. Both **unverified at runtime**.

- 2026-07-31 · round 3c · **One pad binding table, because two of them is what the bug WAS.** With the controller confirmed on player 8, its buttons still did nothing. The plumbing is not the problem and was re-audited end to end this session - `createStreamMessages` emits seat-tagged `MSG_RAW_KEY_DOWN/UP` in the same two-argument shape `Keyboard::createStreamMessages` uses, `WindowXlat` passes non-mouse messages through for seats (`KEEP_MESSAGE`), `MetaEventTranslator` (priority 20) converts them, and the `MSG_META_*` it appends inherits the seat tag through `TheSeatActingSeatOverride`, so it acts on that seat's player. Clicks proving the edges arrive at all (they come from the same `buttonPressed[]` array) is what narrowed it to the table itself.

  **Three buttons were expressed as keys that are bound to nothing.** The legacy seat-0 path sends Y, L3 and R3 as *commands* - `MSG_META_STOP`, `MSG_META_SELECT_NEXT_IDLE_WORKER`, `MSG_META_VIEW_COMMAND_CENTER` - not as keystrokes. The seat table translated them into `KEY_S`, `KEY_E` and `KEY_SPACE`, which only works if CommandMap.ini happens to bind those exact keys to those exact commands. It does not. And a key bound to nothing is *indistinguishable from a button that was never wired*: the message is emitted, every translator ignores it, the pad looks dead. New `seatButtonToMeta` sends the same three commands the legacy path sends, seat-tagged.

  **The shoulder buttons were swapped.** Legacy: LB→`SCANCODE_Q`, RB→`SCANCODE_LSHIFT`. The seat table had LB as the shift modifier and RB as Q - so shift-queue and add-to-selection were on the button the player was not pressing. Both the click-modifier fold in `createStreamMessages` and `getCommandActingShift` in MessageStream now read `SEAT_BUTTON_COMMAND_BAR` (RB), matching seat 0.

  **Start did nothing.** Legacy maps it to Escape. It is the join button, but `routeDeviceInput` only consumes it while the pad is *unseated*, so once bound the edge is free - it now sends `KEY_ESC` like seat 0. **Consequence to decide on:** the quit menu is a single-instance window that `winSeatOwnsWindow` deliberately keeps with seat 0, so a pad seat can now *open* a menu it cannot then press, and it interrupts all eight players. Wired as asked; say the word and it comes back out.

  **The d-pad was doing two jobs.** It emitted control-group keys 1-4 *and* rotated/zoomed that seat's view, so every press did two unrelated things and neither read as deliberate - directly contradicting the comment above it, which already claimed the camera kept the right stick. Control groups only now. Camera rotate/zoom are consequently unbound for a pad on any seat, exactly as they are for a pad on seat 0; the right stick still scrolls, which stays the one deliberate difference from the legacy path.

  **The fix is one table, not a corrected second one.** First pass corrected the copy in SeatManager, which the user rightly rejected: "why are you cloning the controls into seatmanager when you can just refer to the controls in the sdl3 files". Every defect above is a *drift* defect, and a corrected clone drifts again on the next change. So `getSeatButtonBinding()` (declared in `SeatManager.h`, defined next to the seat code) is now the single table both paths read, and each path only decides DELIVERY: seat 0 turns a binding into real OS mouse/keyboard events because it owns the pointer, a seat turns the same binding into seat-tagged messages against its own cursor and viewport. `injectLegacyMouseKeyboard`'s fourteen hand-written `handleGamepadButton` blocks collapse into one loop over the table. Layering allows only this direction - GameEngineDevice may depend on GameEngine, not the reverse - so the table lives with the seat layer and SDL3Input reads it, not the other way round. The one thing the device layer still owns is `translateKeyValToScanCode`, the inverse of the existing scancode translation, needed because the engine speaks DIK and the backend speaks SDL; it `DEBUG_CRASH`es on a key it cannot express rather than dropping it, since a keystroke that goes nowhere is exactly the invisible failure this whole entry is about. `GamepadState::buttonState[]` went with it - the logical edges in `PadEntry::prevLogical` were already being computed for every pad, so there is now one edge detector per pad instead of two.

  Full mapping, now unavoidably identical for every seat: A/B = left/right click, X = `KEY_A`, Y = STOP, LB = `KEY_Q`, RB = shift, Start = Esc, Back = `KEY_SPACE`, d-pad = groups 1-4, L3 = idle worker, R3 = command centre, RT = ctrl, LT = precision. Triggers and sticks stay out of the table: they are levels, not buttons, and the right stick is the one deliberate difference (a seat scrolls its own view directly instead of injecting arrow keys). Debug **and Release** built clean. **Unverified at runtime** - and note this touches seat 0's pad, which previously worked, so that is now a regression risk worth checking first. If a seat's buttons are still dead, the plumbing audit above says the next thing to read is `g_dbgSeatMsgCount[seat]` in the seat overlay: non-zero means the messages are being emitted and the loss is downstream of `MetaEventTranslator`.

- 2026-07-31 · round 3d · **Three fixed, and the pad-button hunt narrowed to one place.**

  **(A) The quit menu is reachable by every seat.** `winSeatOwnsWindow` sends every window that belongs to no control bar to seat 0, which is right for the shell and for dialogs one player opened - but not for this one. The quit menu stops the match for *everyone*, so a pad seat could open a menu it then could not press a single button in while the other seven waited. Non-bar windows still belong to seat 0 except while the quit menu is up. Seat cursors are already released from their viewports for it (`setCursorsUnconfined`), so this is the other half of a thing that was half done. Deliberately shared rather than per seat: whoever clicks Quit quits for all of them, which is what the menu has always meant.

  **(B) The load-screen ally test asked only about player 1's team.** `isSlotLocalAlly` compares against `TheGameInfo->getLocalSlotNum()`, and "the local player" is up to eight people - so player 4's *teammate* was concealed from player 4, on the load screen player 4 is sitting in front of. Split into `isSlotAllyOf(slot, slotIndex, localIndex)` and asked once per local seat: an ally of ANY of them is somebody with a friend at this screen. Non-splitscreen is untouched (the loop finds no claimed seats).

  **(C) One seat's selection and hover lit up in all eight viewports.** Four UI-highlight sites asked `isSelectedByAnySeat()` - true if *any* of the eight selected it - or `getMousedOverDrawableID()` with no argument, which is seat 0's pointer. A viewport belongs to one seat, so both answers are wrong there. New `rts::getRenderSeatIndex()` derives the drawing seat from the render player rather than storing it (so it follows `setRenderPlayerIndexOverride` and there is no second thing to keep in sync; seat 0 is the answer when no other seat's player is being rendered, which covers every single-viewport game). `Drawable::isSelectedOrHoveredByRenderSeat()` uses it and replaces all four. This is the same shape as the selection-flash fix from an earlier round, which had derived the seat inline - that pattern is now named and reusable.

  **(D) Pad buttons: still not fixed, but the search space is much smaller.** The seat's messages ARE being emitted - the camera scroll block sits *after* the message emission in the same loop and scrolling works, so execution provably reaches past every early-out and every `appendMessage`. The tags are set (`friend_setSeatIndex` writes the field `getSeatIndex` reads). So the loss is downstream of emission, in the translator chain, and it takes clicks and keys alike - which is why the pad can move a cursor and scroll a camera and do nothing else. **Prime suspect: commit `83ca81b0a`.** Before it, a seat's mouse messages skipped the window system entirely and always reached the selection/command translators; after it they go through `winProcessMouseEvent`, which returns `WIN_INPUT_USED` for anything it considers handled and WindowXlat then DESTROYs the message. A stuck per-seat `m_grabWindow` or `m_mouseCaptor` would swallow that seat's input permanently and look exactly like this. **Next run answers it without any more code:** the seat overlay already carries `g_dbgSeatMsgCount[seat]` (messages emitted for that seat), `g_dbgLastActiveSeat` (last seat>0 message the stream scoped) and `g_dbgLastClickSeat` (seat tag on the cooked click). Count climbing but last-active-seat stuck at 0 means the tag is not surviving; both climbing but no cooked click means the window system is eating it, and `83ca81b0a` is the thing to revert-test.

  Debug and Release both clean. (A)(B)(C) **unverified at runtime**.

- 2026-07-31 · round 3e · **The pad seat could not pick anything, and that was all of it.** The seat overlay settled it in one screenshot: `msgs=62` for seat 7, `lastActiveSeat=7`, `lastClickSeat=7`. Messages emitted, tag surviving, stream scoping the right seat, cooked click produced with the right seat. So the window system was NOT eating the message and `83ca81b0a` was not the culprit - the loss was after the cooked click, in what the selection and command translators could do with it.

  `View::pickDrawable` opens by asking `getWindowUnderCursor` and returns **nothing at all** if a non-see-through window is under the point. That walk had no seat filter - only `findWindowUnderMouse` (the one WindowXlat uses) had ever been given one. With eight instances of ControlBar.wnd in the manager, a seat clicking inside its own viewport was tested against all eight bars, and a docked bar's windows legitimately extend past the rect they were docked to, because the layout is authored against the whole display and then scaled. One overlap from any other seat's bar and that seat could never select a unit, never issue an order, and never press its own buttons. Its cursor still moved and its camera still scrolled because neither of those goes anywhere near picking - which is exactly the "can't do anything but scroll and move the cursor" shape, and why the button table looked guilty for three rounds.

  Two parts. `getWindowUnderCursor`'s three search loops now apply `winSeatOwnsWindow`, same as `findWindowUnderMouse`. And the seat scope moved **out of WindowXlat and into `MessageStream::propagateMessages`**, around the whole `translateGameMessage` call: it used to be an RAII scope inside the window translator, which unwound it long before the selection and command translators ran, so by picking time the window system had no idea which seat was acting. Nothing else about it changes - seat 0 still swaps nothing (`winEndSeatInput` returns early for seat <= 0), so a single-viewport game runs the same code it always did.

  Lesson worth keeping: three rounds went into the input path on the strength of "the pad does nothing", and the answer was in rendering-adjacent code that input merely calls. The overlay probes that settled it (`g_dbgSeatMsgCount`, `g_dbgLastActiveSeat`, `g_dbgLastClickSeat`) already existed and would have settled it in round one. **Read the probes before theorising.**

  Debug and Release clean. **Unverified at runtime.**

- 2026-07-31 · round 3f · **Stealth, and audio for the observer viewports.**

  **(A) Your own stealth building vanished from your own viewport.** `m_hiddenByStealth` is ONE flag on the drawable, set from a look `StealthUpdate` computes once per frame against `rts::getObservedOrLocalPlayer()` - which during the logic update is the primary local player, player 1. Eight viewports then draw that drawable and all eight obey player 1's answer. So player 8 builds a stealth building, player 1 cannot detect it, and it disappears from player 8's screen. The converse leak was live too and nobody had noticed: player 1's stealthed units were on show in every other seat's viewport. `Drawable::isHiddenByStealthFromRenderPlayer()` decides it per view by OWNERSHIP - never hide a player's own or allied units from them, always hide somebody else's while it is `OBJECT_STATUS_STEALTHED` and not `OBJECT_STATUS_DETECTED`. Ownership is the part of the answer that does not depend on where anyone's detectors are, which is what makes it cheap enough to evaluate at draw time. The opacity and heat-vision treatment `m_stealthLook` drives is left alone: that is a look, not a visibility decision.

  **(B) Audio: observer seats are listeners after all.** `getExtraLocalListeners` excluded them on the reasoning that nobody sits behind one. That is backwards - a viewport that is on screen is one somebody is watching, and under `-splitscreendev 7` SIX of the seven extra viewports are observers, so the machine stayed silent for nearly everything it was showing. Now included. This is the whole of the audio fix: the listener remap, the "any local player" test and the nearest-listener culls from rounds 3/3b were all correct, they were just being handed one seat out of eight.

  **(C) Pad buttons, again - and a strong new datapoint: the pad DOES work in the quit menu.** That exercises emission, seat tagging, stream scoping, `winSeatOwnsWindow`, the hit test and the gadget input path, all for seat 7. So none of those is broken. What the quit menu does NOT exercise is the branch of `winSeatOwnsWindow` that handles a window a control bar owns (`owner == ControlBarInstances::get(m_inputSeat)`) - the quit menu takes the non-bar path, which round 3d opened to every seat. If buttons are still dead on the latest build, that comparison is the next thing to instrument: does the walk find seat 7's own bar as the owner, and does `ControlBarInstances::get(7)` return that same instance. **Check first whether the run that produced this report included `93e441b2a`** - the picking fix landed after the previous report, and it is exactly the kind of thing that changes this symptom.

  Debug and Release clean. All **unverified at runtime**.

- 2026-07-31 · round 3g · **"Select next idle worker jumps to another player" named the whole class.** The user's observation was worth more than the three previous rounds of theory: `InGameUI::selectNextIdleWorker` opens with `rts::getObservedOrLocalPlayer()`, and **that helper follows the RENDER-player override, which is only set while drawing**. During input translation it is unset, so it answers "player 1" no matter which seat pressed the button - the seat got handed player 1's idle-worker list and had its camera thrown across the map. The acting player during translation is `getCommandActingPlayer()`, which has existed since WP5.

  That is a bug CLASS, not one site, and it is the answer to "the pad can't do anything but move a cursor". The pad's buttons were reaching their handlers all along; the handlers were operating on player 1's data, so from the seat's point of view nothing happened. Fixed this round: `selectNextIdleWorker`, `viewCommandCenter` and `iNeedAHero` (all three resolved the player with the render-side helper), plus two of the same shape one level down - `GUICommandTranslator` validated a GUI command's target against **player 1's** relationships, so a seat's abilities rejected targets that were legal for it; and control-group creation collected `drawable->isSelected()`, which is **seat 0's** selection bit, so a pad seat assigned a control group from player 1's selection and got an empty group every time (the acting *player* there was already correct, which is what made it look fine). `areAllSelected` in the drag-select path had the same seat-0 read.

  **The rule, worth stating once:** `rts::getObservedOrLocalPlayer()` answers for the player being DRAWN and is correct in render code only. `getCommandActingPlayer()` / `getCommandActingSeat()` answer for the player whose input is being TRANSLATED. Anything reachable from a translator wants the second pair, and `Drawable::isSelected()` is a seat-0 accessor that should never appear in one. This is bug class 2 from handoff2 §5.2 with a sharper edge than that entry has: the render-side helper is override-aware, so it looks seat-safe, and it silently is not on the input path.

  Also fixed: `Drawable::isHiddenByStealthFromRenderPlayer` (own stealth buildings vanishing - round 3f) and observer seats as audio listeners (round 3f).

  Debug and Release clean. **Unverified at runtime.** Remaining audit, not done: every other `getObservedOrLocalPlayer` / `getLocalPlayer` / `isSelected()` reachable from a translator. A grep-based invariant in `scripts/` would keep this class from coming back.

- 2026-07-31 · round 3h · **The last round's fix exposed a seat-0 accessor next to it.** "gamepad1 lost its idleworker selection" - the acting-player fix was right but landed next to a mismatch. `InGameUI` has three families of legacy no-arg selection accessors and they did not agree: `getSelectCount()`, `selectDrawable()` and `deselectAllDrawables()` follow `m_activeSeat`, while **`getFirstSelectedDrawable()`, `getAllSelectedDrawables()` and `getAllSelectedLocalDrawables()` still hard-coded seat 0**. `selectNextIdleWorker` uses two of them together: with one thing selected on a pad seat and nothing selected on seat 0, `getSelectCount()` answered 1 (so the "exactly one selected" branch ran) and `getFirstSelectedDrawable()` answered null - which the branch then dereferenced. All three now follow `m_activeSeat` like their siblings; `m_activeSeat` is 0 outside translation, so no render-side caller moves.

  The dereference is also guarded independently, because a count and a lookup are two queries and the code should not assume they agree - that assumption is what turned a seat mismatch into a crash rather than a wrong answer.

  Note for the audit: "follows `m_activeSeat`" vs "hard-codes 0" is invisible at every call site, since both spellings are the same no-arg call. Worth a scripted check that every `InGameUI` no-arg accessor with a seat-taking overload forwards `m_activeSeat`.

  Debug and Release clean. **Unverified at runtime.**

- 2026-07-31 · round 3i · **A four-stage trace for the pad command path (user asked for instrumentation, and was right to).** Four rounds have been lost to guessing at this, because every stage of a pad command fails *silently* and looks identical from outside the game. A button press travels: **emit** (`SeatManager::createStreamMessages`) → **scope** (`MessageStream::propagateMessages`) → **handle** (`CommandXlat`) → **act** (`InGameUI`). Each stage now stamps its own row in the seat overlay:

  ```
  META emit=<type>/seat<n> n=<count> | scope=<type>/seat<n> ply=<n> | handle=<type>/seat<n> ply=<n>
  IDLE actPly=<n> list=<n> selCount=<n> result=<n>   (0=ok 1=listEmpty 2=nothingToSelect)
  ```

  How to read it, in order. **emit doesn't move** → the button is not producing a message; the binding table or the edge latch is at fault. **emit moves, scope doesn't** → the message is not reaching the stream, or its seat tag is 0. **scope moves with `ply=-1`** → the acting-player override was NOT installed, and every handler downstream will silently run as player 1 (this is the fault that hid behind rounds 3g/3h). **scope moves, handle doesn't** → a translator ahead of the handler is destroying the message. **handle moves, IDLE doesn't** → the handler ran but never reached the action. **`list=0` with the RIGHT `actPly`** → that player genuinely has no idle worker (every dozer busy), which is a correct no-op and not a broken button — a distinction that is currently invisible, because the function returns in silence and the `DEBUG_CRASH` that would have said so is compiled out of Release.

  Scope-stage recording is restricted to `MSG_BEGIN_META_MESSAGES`..`MSG_END_META_MESSAGES` so the row is not buried under the cursor-position traffic a moving stick generates.

  Also this round: **Release-only builds** from here (see §1) — which is exactly why the trace matters, since asserts do not exist in that configuration.

  **Reported and NOT yet fixed** (diagnosed only): (1) *tire tracks floating between two units* - the track buffer draws in world space with no per-view owner filter, same class as the decal leaks from round 2; (2) *"player has been defeated" appears centrally rather than for the player it concerns* - it is `TheInGameUI->message("GUI:PlayerHasBeenDefeated", ...)` (`VictoryConditions.cpp:210`), the single shared UI message feed, drawn once at one display-relative position. Making that feed per-seat is the fix and is a real chunk of work; it is the last big shared-UI singleton left.

- 2026-07-31 · round 3j · **The tire tracks were my bug, and an input log file.**

  **(A) Tracks stretching between two vehicles and following them.** Not a decal-leak-class filter problem at all - the user's description ("it starts and ends between 2 units, and follows them") described a vertex-buffer offset error, and it was one. `TerrainTracksRenderObjClassSystem::flush()` walks the track list **twice**: the first pass writes each accepted track's vertices into ONE shared vertex buffer in list order, and the second hands out index ranges (`trackStartIndex += m_activeEdgeCount*2`) assuming exactly the same tracks were written. Round 2's per-view fog filter (`trackVisibleToRenderPlayer`) was added to the fill loop **only**. So a track skipped for being in another player's fog still consumed an index range in the draw loop, and every track after it in the list drew from a different unit's vertices - a band of track running between two vehicles and following both, because those vertices are rewritten live every frame. The two conditions must be identical; they now are, with a comment saying so, because a one-sided filter here is silent and looks like a physics bug.

  **(B) `splitscreen_input.log`** in the working directory (`seatLog()`), because the overlay can only show the LAST value of anything and Release has no `DEBUG_LOG`. Neither can answer the two questions that matter: what happened in what order, and **which translator destroyed the message**. Discrete events only (no per-frame spam), truncated per run rather than appended, flushed every line so a crash leaves a usable tail, and self-limiting at 20000 lines. Logs: `EMIT` (button -> meta, with the button index), `BIND`/`TAKEOVER` (including **`playerType`** - `COMPUTER` there means the AI brain is still attached and playing, which is the first thing to check for the "it gets commands to build a worker that nobody gave" report), `xlat#N prio=P <MSG> seat=S actPly=P -> DESTROY|keep` for every translator a seat's meta passes through, and `ACT` for the idle-worker handler. Message names come from `seatMessageName()`, added next to the existing name table in MetaEvent.cpp.

  **What the overlay already told us this round:** `scope=.../seat7 ply=9` and `handle=.../seat7 ply=9` - so the seat tag AND the acting-player override are both correct by the time a handler runs. The `IDLE` row was all `-99`, meaning `selectNextIdleWorker` was never entered. So the idle-worker message specifically is not reaching its handler, while some other meta from seat 7 is. The `xlat#` lines will name the translator that eats it.

- 2026-08-01 · round 3k · **Every control bar was reading seat 0's selection.** Trace now reads `IDLE actPly=9 list=1 selCount=1 result=0` - the idle-worker path is fully correct, and the earlier "select next idle worker jumps to another player" report turned out to be the user confusing it with "view last radar event". Both work. That leaves the bar.

  `ControlBar::evaluateContextUI` (and the observer branch, and `onDrawableDeselected`) ask `TheInGameUI->getSelectCount()` / `getAllSelectedDrawables()` / `getSoloNexusSelectedDrawableID()` / `areSelectedObjectsControllable()` - all no-arg. Round 3h made those follow `m_activeSeat`, which is **correct during message translation and meaningless outside it**: a bar update is a per-frame UI pass, `m_activeSeat` is 0 there, so all eight bars evaluated their context from seat 0's selection. Seat 0 has nothing selected → `CB_CONTEXT_NONE` → no build icons, which is exactly the report. Every one of those calls in ControlBar.cpp now names `m_seatIndex`, which the bar has always known. New seat-taking overloads added for `getSelectCount`, `getSoloNexusSelectedDrawableID` and `areSelectedObjectsControllable` to match the ones `getAllSelectedDrawables` already had.

  `areSelectedObjectsControllable(seat)` deliberately resolves the owner from **the seat's own player** rather than `getCommandActingPlayer()`, for the same reason: it is asked during per-frame bar updates as well as during translation, and outside translation the acting player is player 1.

  **The general shape, now stated twice over:** `m_activeSeat` answers "who is being translated" and is 0 during all drawing and all per-frame updates. `m_seatIndex` on a ControlBar answers "whose bar is this" and is always right. Render and update code wants the second; only translator-reachable code wants the first. This is the same fault as round 3g/3h in a third disguise.

  Still open: **a seat cannot select units by clicking** (A/left click), though it can command an already-selected unit and the cursor/pick plumbing is otherwise live. And the defeat message still draws centrally (shared UI message feed - see round 3i).

  Release builds clean. **Unverified at runtime.** Build note: a link can fail with `LNK1104: cannot open file ... generalszh.exe` purely because the game is still running; compilation had succeeded. Close the game and re-link rather than treating it as a code error.

## 4. Work package status

Legend: `[ ]` not started · `[~]` in progress · `[x]` done+verified · `[!]` blocked (see log)

### WP0 — Seat skeleton
- [x] SeatManager.h/.cpp created, CMake registered (`Core/GameEngine`, both lists)
- [x] Subsystem wired into GameEngine init/reset order (before TheGameClient)
- [x] Debug: seat table logged on init (`SeatManager::logSeatTable`)
- [~] Build green, game boots unchanged — **compiles green** (SeatManager.cpp,
      GameEngine.cpp, SDL3Input.cpp objects built clean, MSVC 2022). Runtime
      boot not yet verified (needs run env, §1).

### WP1 — Gamepad hotplug + debug harness
- [x] Multi-pad map replaces `openFirstGamepad` / single `m_gamepad`
      (`std::map<SDL_JoystickID, PadEntry>` in SDL3Input)
- [x] ADDED/REMOVED events; `onDeviceDisconnected` → `SEAT_DEVICE_LOST`
- [~] Gamepad virtual key/mouse injection **gated, not removed** (see decision
      log 2026-07-22): preserved for flag-off single-player; bypassed when the
      splitscreen flag is on. Full deletion deferred to WP2.
- [x] `SeatInputState` + logical buttons (`Core/GameEngine/Include/GameClient/SeatInput.h`)
- [~] Debug harness (conventions §3): **seat overlay DONE** (`SeatDebugDisplay` in
      SeatManager.cpp, auto-installed via `TheDisplay->setDebugDisplayCallback`
      while the flag is on; shows connected-pad count + per-seat live axes/buttons,
      RTS_DEBUG builds). **Fake seats (Ctrl+Alt+F1..F8) + keyboard-possess
      DEFERRED** — clean impl needs either a small public `Keyboard::isKeyDown`
      accessor (`getKeyStatusData` is protected) or new `MSG_META` keys +
      `CommandMap.ini` entries; user tests with real pads so this is agent-only
      scaffolding, punted to when hardware-less testing is needed.
- [x] `-splitscreendev` command-line flag → `GlobalData::m_splitscreenEnabled` →
      `TheSeatManager->setSplitscreenEnabled()` (parsed in
      `parseCommandLineForEngineInit`, pushed right after the subsystem is created).
      Per-frame `TheSeatManager->UPDATE()` hook added in `GameClient::update`
      after the mouse (WP2's stream-message insertion point).
- [x] Build green; overlay shows live pad state (6 TUs compiled clean, MSVC 2022)

### WP2 — Seat-tagged raw input
- [x] V4, V7, V10 answered (V4 re-confirmed by grep this session: Recorder/NetCommandMsg copy only type/playerIndex/args)
- [x] `GameMessage::m_seatIndex` (client-only, default 0, getter/friend-setter, ctor init; NOT serialized)
- [x] `SeatManager::createStreamMessages()` — integrates the active pad seat's
      virtual cursor, moves the cursor via `TheMouse->setPosition` (SDL3 warps the
      OS cursor so it's visible), and injects seat-tagged `MSG_RAW_MOUSE_*`
      (CONFIRM→left, CANCEL→right, held-CONFIRM+move→drag). Called from
      `GameClient::update` right after the mouse's own `createStreamMessages`.
- [ ] **HC1 human checkpoint**: pad plays a normal skirmish → result: **PENDING (user to test)**

WP2 scope notes / known artifacts to check at HC1:
- Only the **lowest-index bound pad seat** drives the (single, shared) cursor and
  the game for now; seat 0 stays keyboard/mouse; extra pads become independently
  playable at WP5 (seat-aware translators) + WP3 (per-seat cursors). So test with
  **one pad**.
- Translators are not seat-aware yet, so the pad acts as the **local player**
  (its `m_seatIndex` tag is carried but not yet consumed — that's WP5).
- Cursor speed is a fixed per-frame step (`SEAT_CURSOR_STEP=15`, precision on left
  trigger), not dt-scaled yet — feel may need tuning.
- The **A-press that joins a seat** also emits a left-click that same frame (minor;
  can gate later). Right-stick camera pan not wired yet (WP5/LookAtXlat).

### WP3 — Software cursors + tint
- [x] V1, V9 answered (see §2)
- [x] `W3DSeatCursorRenderer` draws all bound seats post-UI (Core/GameEngineDevice
      W3DDevice file; hooked in W3DDisplay::draw after TheMouse->DRAW, RTS_SDL3-gated)
- [x] Per-seat cursor integration in `SeatManager::createStreamMessages` (seats 1..7,
      left-stick step 15px, x0.35 on left trigger, center-seeded, clamped to display)
- [x] Tint = house color (`getNthPlayer(m_playerIndex)->getPlayerColor()`) / 8-color
      fallback palette. **OS cursor hiding for a pad-driven seat 0 intentionally NOT
      done** — WP3 is additive-only (seat 0 stays keyboard/mouse; that is a WP5 item).
- [ ] **HC2 human checkpoint**: multiple tinted cursors → result: **PENDING (user to test)**

WP3 scope notes:
- Cursor art is a small solid tinted right-triangle arrow pointer (hot-spot at the
  tip) with a 1px black outline, drawn via `TheDisplay->drawFillRect`/`drawLine`
  (which route through W3DDisplay's configured Render2DClass batch — Add_Quad/Add_Rect
  modulate by the per-quad vertex color per V1, so the tint needs no shader). Chose
  the Display 2D primitives over a self-managed Render2DClass for robustness (they
  already handle shader/coord-range/state setup). No cursor texture/art assets added.
- Only seats with `m_cursor.visible == TRUE` draw; visible is set only for a bound
  seat with a real device in createStreamMessages, so seat 0 (OS mouse) and unbound
  seats are naturally skipped.
- Test with the splitscreen dev flag ON and ≥1 pad bound to a seat (1..7).

### WP4 — SeatUIContext extraction
- [x] `SeatUIContext` (nested in InGameUI, public) + `m_seatContexts[MAX_SEATS]`;
      `getSeatContext(Int)`; every legacy accessor forwards to seat 0
- [x] Selection, hints, placement, moused-over, UI-mode flags moved into SeatUIContext
- [x] `Drawable` selected-bool → seat mask; callers classified (render=AnySeat, command=seat 0)
- [x] Build green (SDL3 x86, build/win32 Debug): z_gameengine.lib + z_gameenginedevice.lib
      relinked clean; InGameUI.cpp/Drawable.cpp/W3DInGameUI.cpp recompiled with zero NEW
      warnings (only pre-existing C4018/C5055 in untouched functions).
- [ ] Single-player smoke identical (select/box/build/rally) — **PENDING (user to test)**

### WP5 — Seat-aware translators + stamping  (done via a SCOPED approach, not per-site)
- [x] Instead of editing 30+ translator sites, the whole existing translator chain is
      run through a scoped "active seat": `MessageStream::propagateMessages` sets
      `TheInGameUI->setActiveSeat(msg->getSeatIndex())` and a
      `TheSeatActingPlayerOverride` around each `translateGameMessage`, both restored
      immediately after (gated `#if RTS_SDL3_ENABLE`, only for seatIdx>0).
- [x] InGameUI legacy accessors now resolve `m_seatContexts[m_activeSeat]` (was `[0]`);
      m_activeSeat is 0 in all normal/render frames, so single-player is unchanged.
      → SelectionXlat/CommandXlat/PlaceEventTranslator become seat-aware for free
      (they call the same InGameUI methods; picking reads the msg pixel arg, which
      carries the seat cursor pos).
- [x] Command stamping: `GameMessage` ctor uses `TheSeatActingPlayerOverride` when set,
      so commands the translators create during a seat message are attributed to that
      seat's player. (No global setLocalPlayer swap — it fires becomingLocalPlayer.)
- [x] Seat message emission re-added to `SeatManager::createStreamMessages` (cursor +
      A=left / B=right, seat-tagged); a bound pad no longer drives the OS mouse
      (SDL3 `continue`s past legacy injection).
- [x] Dev seat→player mapping (no lobby yet = WP9): seat k → getNthPlayer(k) once a
      match is running. **Caveat: that player may be an AI that fights the controller;
      a clean human player 2 needs WP9.**
- [x] Build green (SDL3 x86): z_gameengine.lib + z_gameenginedevice.lib link clean.
- [ ] NOT done this pass: LookAtXlat camera per-seat (right-stick pan), WindowXlat
      seat-0 guard, per-site dispatcher assert, replay verify, meta/hotkey buttons for
      the controller (Y/D-pad/etc. — only cursor + A/B emit for a bound seat now).
- [ ] **HC3 human checkpoint**: controller selects & commands its mapped player's army
      independently of the mouse → result: **PENDING (user to test)**

### WP6 — Viewport layout
- [ ] V6 answered
- [ ] `ViewportLayout.*`: layout table, apply/re-flow, seat→view assignment
- [ ] `View::m_renderPlayerIndex`; seat 0 view == TheTacticalView (resized, never destroyed)
- [ ] Dividers; letterbox disabled for multi-seat
- [ ] 1/2/4/8 fake-seat layouts render; single-seat pixel-identical

### WP7 — Per-viewport render player
- [ ] **Spike A first** (2 views, rows 1–3 of plan2 WP7 table) → result: ____
- [ ] `ScopedRenderPlayer` RAII wraps per-view draw
- [ ] W3DScene hiding · particles · shroud textures per player
- [ ] Ghost objects multi-local (storage per index, render-select, save/load strategy decided → log)
- [ ] Stealth/occlusion sweep of W3DDevice drawable modules
- [ ] **HC4 human checkpoint**: per-viewport fog & ghosts correct → result: ____

### WP8 — ControlBar instancing
- [ ] V2, V8 answered
- [ ] **Spike C first**: 2 instances, bottom + mirrored top, clickable → result: ____
- [ ] Instance array + `TheControlBar` = instance 0; window-pointer cache per instance
- [ ] All `"ControlBar.wnd:*"` lookups scoped (grep shows zero nullptr-parent lookups)
- [ ] Callback routing via `fromWindow`; 30 `getLocalPlayer` sites → `m_player`
- [ ] Geometry: dock/scale/mirror transforms; UV-flip bar art
- [ ] Per-seat window input hit-testing (lifts WP5 guard)
- [ ] Radar per instance
- [ ] **HC5 human checkpoint**: mirrored bar fully functional → result: ____

### WP9 — Join/leave + transport
- [ ] V5 (Spike B) answered → result: ____
- [ ] Lobby: join claims open→bot slots (restore state remembered); leave restores; slot UI updates
- [ ] Per-seat lobby cursors confined to own slot row
- [ ] Match start: seat→playerIndex map; layout applied
- [ ] `MSG_LOGIC_LOCAL_CONTROL_RELEASE/TAKE` + dispatcher cases + client bind/unbind/re-flow
- [ ] `CommandTransport` scaffolding (separate commit, passthrough-only)
- [ ] Network guards: bind refused when networked; layout asserts 1 seat
- [ ] **HC6 human checkpoint**: full join/leave loop + LAN regression → result: ____

## 5. Known deferred items (do not implement)

Mouse/keyboard seats (InputRoute exists, unused) · shared-screen/co-op UI
exposure · splitscreen-over-LAN · full frontend controller nav · per-seat group
hotkeys · Generals (non-ZH) port.
